---
# Home Assistant Ultimate Theme
# GENERATED FILE - edit build/modes.py + build/template.jinja2 and
# re-run build/generate_themes.py instead.
# 154 themes across 11 aesthetics.
# Backgrounds: cdn

#
# Ultimate Glass
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #1b1030 0%, #0b0d14 55%, #12202e 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #1b1030 0%, #0b0d14 55%, #12202e 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#042e59"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#0A84FF"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#0A84FF"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#011020"
  ha-color-primary-10: "#022140"
  ha-color-primary-20: "#054280"
  ha-color-primary-30: "#0863bf"
  ha-color-primary-40: "#0A84FF"
  ha-color-primary-50: "#3398ff"
  ha-color-primary-60: "#5cadff"
  ha-color-primary-70: "#84c2ff"
  ha-color-primary-80: "#add6ff"
  ha-color-primary-90: "#d6eaff"
  ha-color-primary-95: "#ebf5ff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Ember":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#250406"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#FF453A"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF453A"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#200907"
  ha-color-primary-10: "#40110e"
  ha-color-primary-20: "#80221d"
  ha-color-primary-30: "#bf342c"
  ha-color-primary-40: "#FF453A"
  ha-color-primary-50: "#ff645b"
  ha-color-primary-60: "#ff837c"
  ha-color-primary-70: "#ffa29c"
  ha-color-primary-80: "#ffc1bd"
  ha-color-primary-90: "#ffe0de"
  ha-color-primary-95: "#fff0ef"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Rose":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#2a0820"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#FF375F"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF375F"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#20070c"
  ha-color-primary-10: "#400e18"
  ha-color-primary-20: "#801c30"
  ha-color-primary-30: "#bf2947"
  ha-color-primary-40: "#FF375F"
  ha-color-primary-50: "#ff587a"
  ha-color-primary-60: "#ff7a94"
  ha-color-primary-70: "#ff9baf"
  ha-color-primary-80: "#ffbcca"
  ha-color-primary-90: "#ffdee4"
  ha-color-primary-95: "#ffeef2"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Amber":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1e1b1a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#FF9F0A"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF9F0A"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#201401"
  ha-color-primary-10: "#402802"
  ha-color-primary-20: "#805005"
  ha-color-primary-30: "#bf7708"
  ha-color-primary-40: "#FF9F0A"
  ha-color-primary-50: "#ffaf33"
  ha-color-primary-60: "#ffbf5c"
  ha-color-primary-70: "#ffcf84"
  ha-color-primary-80: "#ffdfad"
  ha-color-primary-90: "#ffefd6"
  ha-color-primary-95: "#fff7eb"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Citrine":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#2a1705"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#FFD60A"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFD60A"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#201b01"
  ha-color-primary-10: "#403602"
  ha-color-primary-20: "#806b05"
  ha-color-primary-30: "#bfa008"
  ha-color-primary-40: "#FFD60A"
  ha-color-primary-50: "#ffdd33"
  ha-color-primary-60: "#ffe45c"
  ha-color-primary-70: "#ffea84"
  ha-color-primary-80: "#fff1ad"
  ha-color-primary-90: "#fff8d6"
  ha-color-primary-95: "#fffceb"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Lime":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#20320f"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#B4E33D"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4E33D"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#161c08"
  ha-color-primary-10: "#2d390f"
  ha-color-primary-20: "#5a721e"
  ha-color-primary-30: "#87aa2e"
  ha-color-primary-40: "#B4E33D"
  ha-color-primary-50: "#c0e85d"
  ha-color-primary-60: "#cdec7e"
  ha-color-primary-70: "#daf19e"
  ha-color-primary-80: "#e6f6be"
  ha-color-primary-90: "#f2fadf"
  ha-color-primary-95: "#f9fdef"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Verdant":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1f2f12"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#32D74B"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#32D74B"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#061b09"
  ha-color-primary-10: "#0c3613"
  ha-color-primary-20: "#196c26"
  ha-color-primary-30: "#26a138"
  ha-color-primary-40: "#32D74B"
  ha-color-primary-50: "#54de69"
  ha-color-primary-60: "#76e487"
  ha-color-primary-70: "#98eba5"
  ha-color-primary-80: "#bbf2c3"
  ha-color-primary-90: "#ddf8e1"
  ha-color-primary-95: "#eefcf0"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Jade":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1a2d22"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#5FD0B0"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#5FD0B0"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#0c1a16"
  ha-color-primary-10: "#18342c"
  ha-color-primary-20: "#306858"
  ha-color-primary-30: "#479c84"
  ha-color-primary-40: "#5FD0B0"
  ha-color-primary-50: "#7ad8bd"
  ha-color-primary-60: "#94e0ca"
  ha-color-primary-70: "#afe8d8"
  ha-color-primary-80: "#caefe5"
  ha-color-primary-90: "#e4f7f2"
  ha-color-primary-95: "#f2fbf8"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Lagoon":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#022d36"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#5AC8F5"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#5AC8F5"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#0b191f"
  ha-color-primary-10: "#16323d"
  ha-color-primary-20: "#2d647a"
  ha-color-primary-30: "#4496b8"
  ha-color-primary-40: "#5AC8F5"
  ha-color-primary-50: "#76d1f7"
  ha-color-primary-60: "#91daf8"
  ha-color-primary-70: "#ace4fa"
  ha-color-primary-80: "#c8edfc"
  ha-color-primary-90: "#e4f6fd"
  ha-color-primary-95: "#f1fafe"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Azure":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#011b31"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#00C2FF"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00C2FF"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#001820"
  ha-color-primary-10: "#003040"
  ha-color-primary-20: "#006180"
  ha-color-primary-30: "#0092bf"
  ha-color-primary-40: "#00C2FF"
  ha-color-primary-50: "#2accff"
  ha-color-primary-60: "#55d6ff"
  ha-color-primary-70: "#80e0ff"
  ha-color-primary-80: "#aaebff"
  ha-color-primary-90: "#d4f5ff"
  ha-color-primary-95: "#eafaff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Cobalt":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0f2435"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#0A84FF"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#0A84FF"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#011020"
  ha-color-primary-10: "#022140"
  ha-color-primary-20: "#054280"
  ha-color-primary-30: "#0863bf"
  ha-color-primary-40: "#0A84FF"
  ha-color-primary-50: "#3398ff"
  ha-color-primary-60: "#5cadff"
  ha-color-primary-70: "#84c2ff"
  ha-color-primary-80: "#add6ff"
  ha-color-primary-90: "#d6eaff"
  ha-color-primary-95: "#ebf5ff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Indigo":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0b0c2e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#9B8CFF"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#9B8CFF"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#131220"
  ha-color-primary-10: "#272340"
  ha-color-primary-20: "#4e4680"
  ha-color-primary-30: "#7469bf"
  ha-color-primary-40: "#9B8CFF"
  ha-color-primary-50: "#ac9fff"
  ha-color-primary-60: "#bcb2ff"
  ha-color-primary-70: "#cdc6ff"
  ha-color-primary-80: "#ded9ff"
  ha-color-primary-90: "#eeecff"
  ha-color-primary-95: "#f7f5ff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Violet":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#16082c"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#BF5AF2"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#BF5AF2"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#180b1e"
  ha-color-primary-10: "#30163c"
  ha-color-primary-20: "#602d79"
  ha-color-primary-30: "#8f44b6"
  ha-color-primary-40: "#BF5AF2"
  ha-color-primary-50: "#ca76f4"
  ha-color-primary-60: "#d491f6"
  ha-color-primary-70: "#dfacf8"
  ha-color-primary-80: "#eac8fb"
  ha-color-primary-90: "#f4e4fd"
  ha-color-primary-95: "#faf1fe"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Sand":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#342a24"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#B8A38A"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B8A38A"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#171411"
  ha-color-primary-10: "#2e2922"
  ha-color-primary-20: "#5c5245"
  ha-color-primary-30: "#8a7a68"
  ha-color-primary-40: "#B8A38A"
  ha-color-primary-50: "#c4b29e"
  ha-color-primary-60: "#d0c2b1"
  ha-color-primary-70: "#dcd1c4"
  ha-color-primary-80: "#e7e0d8"
  ha-color-primary-90: "#f3f0ec"
  ha-color-primary-95: "#f9f7f5"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Glass - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Glass - Graphite":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/glass/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1e2025"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#98989D"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#98989D"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#131314"
  ha-color-primary-10: "#262627"
  ha-color-primary-20: "#4c4c4e"
  ha-color-primary-30: "#727276"
  ha-color-primary-40: "#98989D"
  ha-color-primary-50: "#a9a9ad"
  ha-color-primary-60: "#bababe"
  ha-color-primary-70: "#ccccce"
  ha-color-primary-80: "#ddddde"
  ha-color-primary-90: "#eeeeef"
  ha-color-primary-95: "#f6f6f7"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Glass - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #313244 0%, #1e1e2e 55%, #181825 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #313244 0%, #1e1e2e 55%, #181825 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#303f58"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#89b4fa"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#89b4fa"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#11161f"
  ha-color-primary-10: "#222d3e"
  ha-color-primary-20: "#445a7d"
  ha-color-primary-30: "#6787bc"
  ha-color-primary-40: "#89b4fa"
  ha-color-primary-50: "#9dc0fb"
  ha-color-primary-60: "#b0cdfc"
  ha-color-primary-70: "#c4dafc"
  ha-color-primary-80: "#d8e6fd"
  ha-color-primary-90: "#ebf2fe"
  ha-color-primary-95: "#f5f9ff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Ember":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#20111a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F2CDCD"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F2CDCD"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1e1a1a"
  ha-color-primary-10: "#3c3333"
  ha-color-primary-20: "#796666"
  ha-color-primary-30: "#b69a9a"
  ha-color-primary-40: "#F2CDCD"
  ha-color-primary-50: "#f4d5d5"
  ha-color-primary-60: "#f6dede"
  ha-color-primary-70: "#f8e6e6"
  ha-color-primary-80: "#fbeeee"
  ha-color-primary-90: "#fdf7f7"
  ha-color-primary-95: "#fefbfb"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Rose":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#271c2e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#EBA0AC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#EBA0AC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1d1416"
  ha-color-primary-10: "#3b282b"
  ha-color-primary-20: "#765056"
  ha-color-primary-30: "#b07881"
  ha-color-primary-40: "#EBA0AC"
  ha-color-primary-50: "#eeb0ba"
  ha-color-primary-60: "#f2c0c8"
  ha-color-primary-70: "#f5d0d6"
  ha-color-primary-80: "#f8dfe3"
  ha-color-primary-90: "#fceff1"
  ha-color-primary-95: "#fdf7f8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Amber":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#28222d"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Citrine":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#221c26"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Lime":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#2b2929"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Verdant":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#28282a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#A6E3A1"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#A6E3A1"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#151c14"
  ha-color-primary-10: "#2a3928"
  ha-color-primary-20: "#537250"
  ha-color-primary-30: "#7caa79"
  ha-color-primary-40: "#A6E3A1"
  ha-color-primary-50: "#b5e8b1"
  ha-color-primary-60: "#c4ecc0"
  ha-color-primary-70: "#d2f1d0"
  ha-color-primary-80: "#e1f6e0"
  ha-color-primary-90: "#f0faef"
  ha-color-primary-95: "#f8fdf7"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Jade":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1d232a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#94E2D5"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#94E2D5"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#121c1b"
  ha-color-primary-10: "#253835"
  ha-color-primary-20: "#4a716a"
  ha-color-primary-30: "#6faaa0"
  ha-color-primary-40: "#94E2D5"
  ha-color-primary-50: "#a6e7dc"
  ha-color-primary-60: "#b8ece3"
  ha-color-primary-70: "#caf0ea"
  ha-color-primary-80: "#dbf5f1"
  ha-color-primary-90: "#edfaf8"
  ha-color-primary-95: "#f6fdfc"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Lagoon":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1a293a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#74C7EC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#74C7EC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#0e191e"
  ha-color-primary-10: "#1d323b"
  ha-color-primary-20: "#3a6476"
  ha-color-primary-30: "#5795b1"
  ha-color-primary-40: "#74C7EC"
  ha-color-primary-50: "#8bd0ef"
  ha-color-primary-60: "#a2daf2"
  ha-color-primary-70: "#bae3f6"
  ha-color-primary-80: "#d1ecf9"
  ha-color-primary-90: "#e8f6fc"
  ha-color-primary-95: "#f3fafd"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Azure":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#161728"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#74C7EC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#74C7EC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#0e191e"
  ha-color-primary-10: "#1d323b"
  ha-color-primary-20: "#3a6476"
  ha-color-primary-30: "#5795b1"
  ha-color-primary-40: "#74C7EC"
  ha-color-primary-50: "#8bd0ef"
  ha-color-primary-60: "#a2daf2"
  ha-color-primary-70: "#bae3f6"
  ha-color-primary-80: "#d1ecf9"
  ha-color-primary-90: "#e8f6fc"
  ha-color-primary-95: "#f3fafd"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Cobalt":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#262838"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#89B4FA"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#89B4FA"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#11161f"
  ha-color-primary-10: "#222d3e"
  ha-color-primary-20: "#445a7d"
  ha-color-primary-30: "#6787bc"
  ha-color-primary-40: "#89B4FA"
  ha-color-primary-50: "#9dc0fb"
  ha-color-primary-60: "#b0cdfc"
  ha-color-primary-70: "#c4dafc"
  ha-color-primary-80: "#d8e6fd"
  ha-color-primary-90: "#ebf2fe"
  ha-color-primary-95: "#f5f9ff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Indigo":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1b1a2c"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#B4BEFE"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4BEFE"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#161820"
  ha-color-primary-10: "#2d3040"
  ha-color-primary-20: "#5a5f7f"
  ha-color-primary-30: "#878ebe"
  ha-color-primary-40: "#B4BEFE"
  ha-color-primary-50: "#c0c9fe"
  ha-color-primary-60: "#cdd4fe"
  ha-color-primary-70: "#dadefe"
  ha-color-primary-80: "#e6e9ff"
  ha-color-primary-90: "#f2f4ff"
  ha-color-primary-95: "#f9faff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Violet":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#171426"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#CBA6F7"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#CBA6F7"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#19151f"
  ha-color-primary-10: "#332a3e"
  ha-color-primary-20: "#66537c"
  ha-color-primary-30: "#987cb9"
  ha-color-primary-40: "#CBA6F7"
  ha-color-primary-50: "#d4b5f8"
  ha-color-primary-60: "#dcc4fa"
  ha-color-primary-70: "#e5d2fb"
  ha-color-primary-80: "#eee1fc"
  ha-color-primary-90: "#f6f0fe"
  ha-color-primary-95: "#fbf8fe"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Sand":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#2d232e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Velvet - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Velvet - Graphite":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/velvet/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1e1c23"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#B4BEFE"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4BEFE"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#161820"
  ha-color-primary-10: "#2d3040"
  ha-color-primary-20: "#5a5f7f"
  ha-color-primary-30: "#878ebe"
  ha-color-primary-40: "#B4BEFE"
  ha-color-primary-50: "#c0c9fe"
  ha-color-primary-60: "#cdd4fe"
  ha-color-primary-70: "#dadefe"
  ha-color-primary-80: "#e6e9ff"
  ha-color-primary-90: "#f2f4ff"
  ha-color-primary-95: "#f9faff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Velvet - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #12002b 0%, #04050a 55%, #001a2b 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #12002b 0%, #04050a 55%, #001a2b 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#003959"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00A3FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00A3FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001420"
  ha-color-primary-10: "#002940"
  ha-color-primary-20: "#005280"
  ha-color-primary-30: "#007abf"
  ha-color-primary-40: "#00A3FF"
  ha-color-primary-50: "#2ab2ff"
  ha-color-primary-60: "#55c2ff"
  ha-color-primary-70: "#80d1ff"
  ha-color-primary-80: "#aae0ff"
  ha-color-primary-90: "#d4f0ff"
  ha-color-primary-95: "#eaf7ff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Ember":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0d0002"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF0E00"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF0E00"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#200200"
  ha-color-primary-10: "#400400"
  ha-color-primary-20: "#800700"
  ha-color-primary-30: "#bf0a00"
  ha-color-primary-40: "#FF0E00"
  ha-color-primary-50: "#ff362a"
  ha-color-primary-60: "#ff5e55"
  ha-color-primary-70: "#ff8680"
  ha-color-primary-80: "#ffafaa"
  ha-color-primary-90: "#ffd7d4"
  ha-color-primary-95: "#ffebea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Rose":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1a0120"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF0033"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF0033"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#200006"
  ha-color-primary-10: "#40000d"
  ha-color-primary-20: "#80001a"
  ha-color-primary-30: "#bf0026"
  ha-color-primary-40: "#FF0033"
  ha-color-primary-50: "#ff2a55"
  ha-color-primary-60: "#ff5577"
  ha-color-primary-70: "#ff8099"
  ha-color-primary-80: "#ffaabb"
  ha-color-primary-90: "#ffd4dd"
  ha-color-primary-95: "#ffeaee"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Amber":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#130e12"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF9B00"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF9B00"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201300"
  ha-color-primary-10: "#402700"
  ha-color-primary-20: "#804e00"
  ha-color-primary-30: "#bf7400"
  ha-color-primary-40: "#FF9B00"
  ha-color-primary-50: "#ffac2a"
  ha-color-primary-60: "#ffbc55"
  ha-color-primary-70: "#ffcd80"
  ha-color-primary-80: "#ffdeaa"
  ha-color-primary-90: "#ffeed4"
  ha-color-primary-95: "#fff7ea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Citrine":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0f0805"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FFD400"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFD400"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201a00"
  ha-color-primary-10: "#403500"
  ha-color-primary-20: "#806a00"
  ha-color-primary-30: "#bf9f00"
  ha-color-primary-40: "#FFD400"
  ha-color-primary-50: "#ffdb2a"
  ha-color-primary-60: "#ffe255"
  ha-color-primary-70: "#ffea80"
  ha-color-primary-80: "#fff1aa"
  ha-color-primary-90: "#fff8d4"
  ha-color-primary-95: "#fffbea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Lime":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#101805"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#B8FF03"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B8FF03"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#172000"
  ha-color-primary-10: "#2e4001"
  ha-color-primary-20: "#5c8002"
  ha-color-primary-30: "#8abf02"
  ha-color-primary-40: "#B8FF03"
  ha-color-primary-50: "#c4ff2d"
  ha-color-primary-60: "#d0ff57"
  ha-color-primary-70: "#dcff81"
  ha-color-primary-80: "#e7ffab"
  ha-color-primary-90: "#f3ffd5"
  ha-color-primary-95: "#f9ffea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Verdant":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#061305"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00FF27"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00FF27"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#002005"
  ha-color-primary-10: "#00400a"
  ha-color-primary-20: "#008014"
  ha-color-primary-30: "#00bf1d"
  ha-color-primary-40: "#00FF27"
  ha-color-primary-50: "#2aff4b"
  ha-color-primary-60: "#55ff6f"
  ha-color-primary-70: "#80ff93"
  ha-color-primary-80: "#aaffb7"
  ha-color-primary-90: "#d4ffdb"
  ha-color-primary-95: "#eaffed"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Jade":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#020e0e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#38FFC7"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#38FFC7"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#072019"
  ha-color-primary-10: "#0e4032"
  ha-color-primary-20: "#1c8064"
  ha-color-primary-30: "#2abf95"
  ha-color-primary-40: "#38FFC7"
  ha-color-primary-50: "#59ffd0"
  ha-color-primary-60: "#7affda"
  ha-color-primary-70: "#9cffe3"
  ha-color-primary-80: "#bdffec"
  ha-color-primary-90: "#defff6"
  ha-color-primary-95: "#eefffa"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Lagoon":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#011319"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#25C0FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#25C0FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#051820"
  ha-color-primary-10: "#093040"
  ha-color-primary-20: "#126080"
  ha-color-primary-30: "#1c90bf"
  ha-color-primary-40: "#25C0FF"
  ha-color-primary-50: "#49caff"
  ha-color-primary-60: "#6ed5ff"
  ha-color-primary-70: "#92e0ff"
  ha-color-primary-80: "#b6eaff"
  ha-color-primary-90: "#dbf4ff"
  ha-color-primary-95: "#edfaff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Azure":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#020a1a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00C2FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00C2FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001820"
  ha-color-primary-10: "#003040"
  ha-color-primary-20: "#006180"
  ha-color-primary-30: "#0092bf"
  ha-color-primary-40: "#00C2FF"
  ha-color-primary-50: "#2accff"
  ha-color-primary-60: "#55d6ff"
  ha-color-primary-70: "#80e0ff"
  ha-color-primary-80: "#aaebff"
  ha-color-primary-90: "#d4f5ff"
  ha-color-primary-95: "#eafaff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Cobalt":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#06101a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#007FFF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#007FFF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001020"
  ha-color-primary-10: "#002040"
  ha-color-primary-20: "#004080"
  ha-color-primary-30: "#005fbf"
  ha-color-primary-40: "#007FFF"
  ha-color-primary-50: "#2a94ff"
  ha-color-primary-60: "#55aaff"
  ha-color-primary-70: "#80bfff"
  ha-color-primary-80: "#aad4ff"
  ha-color-primary-90: "#d4eaff"
  ha-color-primary-95: "#eaf4ff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Indigo":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a041a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#5238FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#5238FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#0a0720"
  ha-color-primary-10: "#140e40"
  ha-color-primary-20: "#291c80"
  ha-color-primary-30: "#3e2abf"
  ha-color-primary-40: "#5238FF"
  ha-color-primary-50: "#6f59ff"
  ha-color-primary-60: "#8c7aff"
  ha-color-primary-70: "#a89cff"
  ha-color-primary-80: "#c5bdff"
  ha-color-primary-90: "#e2deff"
  ha-color-primary-95: "#f1eeff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Violet":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#080212"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#B627FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B627FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#170520"
  ha-color-primary-10: "#2e0a40"
  ha-color-primary-20: "#5b1480"
  ha-color-primary-30: "#881dbf"
  ha-color-primary-40: "#B627FF"
  ha-color-primary-50: "#c24bff"
  ha-color-primary-60: "#ce6fff"
  ha-color-primary-70: "#da93ff"
  ha-color-primary-80: "#e7b7ff"
  ha-color-primary-90: "#f3dbff"
  ha-color-primary-95: "#f9edff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Sand":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#110d12"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FFA438"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFA438"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201407"
  ha-color-primary-10: "#40290e"
  ha-color-primary-20: "#80521c"
  ha-color-primary-30: "#bf7b2a"
  ha-color-primary-40: "#FFA438"
  ha-color-primary-50: "#ffb359"
  ha-color-primary-60: "#ffc27a"
  ha-color-primary-70: "#ffd29c"
  ha-color-primary-80: "#ffe1bd"
  ha-color-primary-90: "#fff0de"
  ha-color-primary-95: "#fff7ee"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Neon - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Neon - Graphite":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/neon/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#040508"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#3838FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#3838FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#070720"
  ha-color-primary-10: "#0e0e40"
  ha-color-primary-20: "#1c1c80"
  ha-color-primary-30: "#2a2abf"
  ha-color-primary-40: "#3838FF"
  ha-color-primary-50: "#5959ff"
  ha-color-primary-60: "#7a7aff"
  ha-color-primary-70: "#9c9cff"
  ha-color-primary-80: "#bdbdff"
  ha-color-primary-90: "#dedeff"
  ha-color-primary-95: "#eeeeff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Neon - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Ionut
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Ionut":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #1b1030 0%, #0b0d14 55%, #12202e 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #1b1030 0%, #0b0d14 55%, #12202e 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#042e59"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#0A84FF"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#0A84FF"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#011020"
  ha-color-primary-10: "#022140"
  ha-color-primary-20: "#054280"
  ha-color-primary-30: "#0863bf"
  ha-color-primary-40: "#0A84FF"
  ha-color-primary-50: "#3398ff"
  ha-color-primary-60: "#5cadff"
  ha-color-primary-70: "#84c2ff"
  ha-color-primary-80: "#add6ff"
  ha-color-primary-90: "#d6eaff"
  ha-color-primary-95: "#ebf5ff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Ionut"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Ionut - Circuit
# Area: Circuit  (mdi:chip)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Ionut - Circuit":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/ionut/circuit.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/ionut/circuit.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#031333"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#0063FD"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#0063FD"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#000c20"
  ha-color-primary-10: "#00193f"
  ha-color-primary-20: "#00327e"
  ha-color-primary-30: "#004abe"
  ha-color-primary-40: "#0063FD"
  ha-color-primary-50: "#2a7dfd"
  ha-color-primary-60: "#5597fe"
  ha-color-primary-70: "#80b1fe"
  ha-color-primary-80: "#aacbfe"
  ha-color-primary-90: "#d4e5ff"
  ha-color-primary-95: "#eaf2ff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Ionut - Circuit"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Ionut - Facet
# Area: Facet  (mdi:triangle-outline)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Ionut - Facet":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/ionut/facet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/ionut/facet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a224a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#1772ED"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#1772ED"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#030e1e"
  ha-color-primary-10: "#061c3b"
  ha-color-primary-20: "#0c3976"
  ha-color-primary-30: "#1156b2"
  ha-color-primary-40: "#1772ED"
  ha-color-primary-50: "#3e8af0"
  ha-color-primary-60: "#64a1f3"
  ha-color-primary-70: "#8bb8f6"
  ha-color-primary-80: "#b2d0f9"
  ha-color-primary-90: "#d8e8fc"
  ha-color-primary-95: "#ecf3fe"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Ionut - Facet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Ionut - Fusion
# Area: Fusion  (mdi:hexagon-multiple)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Ionut - Fusion":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/ionut/fusion.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/ionut/fusion.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#04112b"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#1E51A6"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#1E51A6"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#040a15"
  ha-color-primary-10: "#08142a"
  ha-color-primary-20: "#0f2853"
  ha-color-primary-30: "#163d7c"
  ha-color-primary-40: "#1E51A6"
  ha-color-primary-50: "#446eb5"
  ha-color-primary-60: "#698bc4"
  ha-color-primary-70: "#8ea8d2"
  ha-color-primary-80: "#b4c5e1"
  ha-color-primary-90: "#dae2f0"
  ha-color-primary-95: "#ecf0f8"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Ionut - Fusion"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #1b1030 0%, #0b0d14 55%, #12202e 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #1b1030 0%, #0b0d14 55%, #12202e 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#042e59"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#0A84FF"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#0A84FF"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#011020"
  ha-color-primary-10: "#022140"
  ha-color-primary-20: "#054280"
  ha-color-primary-30: "#0863bf"
  ha-color-primary-40: "#0A84FF"
  ha-color-primary-50: "#3398ff"
  ha-color-primary-60: "#5cadff"
  ha-color-primary-70: "#84c2ff"
  ha-color-primary-80: "#add6ff"
  ha-color-primary-90: "#d6eaff"
  ha-color-primary-95: "#ebf5ff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Ember":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#140c10"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#FF453A"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF453A"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#200907"
  ha-color-primary-10: "#40110e"
  ha-color-primary-20: "#80221d"
  ha-color-primary-30: "#bf342c"
  ha-color-primary-40: "#FF453A"
  ha-color-primary-50: "#ff645b"
  ha-color-primary-60: "#ff837c"
  ha-color-primary-70: "#ffa29c"
  ha-color-primary-80: "#ffc1bd"
  ha-color-primary-90: "#ffe0de"
  ha-color-primary-95: "#fff0ef"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Rose":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0f0e18"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#FF375F"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF375F"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#20070c"
  ha-color-primary-10: "#400e18"
  ha-color-primary-20: "#801c30"
  ha-color-primary-30: "#bf2947"
  ha-color-primary-40: "#FF375F"
  ha-color-primary-50: "#ff587a"
  ha-color-primary-60: "#ff7a94"
  ha-color-primary-70: "#ff9baf"
  ha-color-primary-80: "#ffbcca"
  ha-color-primary-90: "#ffdee4"
  ha-color-primary-95: "#ffeef2"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Amber":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a0e11"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#FF9F0A"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF9F0A"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#201401"
  ha-color-primary-10: "#402802"
  ha-color-primary-20: "#805005"
  ha-color-primary-30: "#bf7708"
  ha-color-primary-40: "#FF9F0A"
  ha-color-primary-50: "#ffaf33"
  ha-color-primary-60: "#ffbf5c"
  ha-color-primary-70: "#ffcf84"
  ha-color-primary-80: "#ffdfad"
  ha-color-primary-90: "#ffefd6"
  ha-color-primary-95: "#fff7eb"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Citrine":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#131312"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#FFD60A"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFD60A"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#201b01"
  ha-color-primary-10: "#403602"
  ha-color-primary-20: "#806b05"
  ha-color-primary-30: "#bfa008"
  ha-color-primary-40: "#FFD60A"
  ha-color-primary-50: "#ffdd33"
  ha-color-primary-60: "#ffe45c"
  ha-color-primary-70: "#ffea84"
  ha-color-primary-80: "#fff1ad"
  ha-color-primary-90: "#fff8d6"
  ha-color-primary-95: "#fffceb"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Lime":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0e1910"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#B4E33D"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4E33D"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#161c08"
  ha-color-primary-10: "#2d390f"
  ha-color-primary-20: "#5a721e"
  ha-color-primary-30: "#87aa2e"
  ha-color-primary-40: "#B4E33D"
  ha-color-primary-50: "#c0e85d"
  ha-color-primary-60: "#cdec7e"
  ha-color-primary-70: "#daf19e"
  ha-color-primary-80: "#e6f6be"
  ha-color-primary-90: "#f2fadf"
  ha-color-primary-95: "#f9fdef"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Verdant":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0d1511"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#32D74B"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#32D74B"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#061b09"
  ha-color-primary-10: "#0c3613"
  ha-color-primary-20: "#196c26"
  ha-color-primary-30: "#26a138"
  ha-color-primary-40: "#32D74B"
  ha-color-primary-50: "#54de69"
  ha-color-primary-60: "#76e487"
  ha-color-primary-70: "#98eba5"
  ha-color-primary-80: "#bbf2c3"
  ha-color-primary-90: "#ddf8e1"
  ha-color-primary-95: "#eefcf0"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Jade":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a1316"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#5FD0B0"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#5FD0B0"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#0c1a16"
  ha-color-primary-10: "#18342c"
  ha-color-primary-20: "#306858"
  ha-color-primary-30: "#479c84"
  ha-color-primary-40: "#5FD0B0"
  ha-color-primary-50: "#7ad8bd"
  ha-color-primary-60: "#94e0ca"
  ha-color-primary-70: "#afe8d8"
  ha-color-primary-80: "#caefe5"
  ha-color-primary-90: "#e4f7f2"
  ha-color-primary-95: "#f2fbf8"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Lagoon":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#071014"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#5AC8F5"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#5AC8F5"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#0b191f"
  ha-color-primary-10: "#16323d"
  ha-color-primary-20: "#2d647a"
  ha-color-primary-30: "#4496b8"
  ha-color-primary-40: "#5AC8F5"
  ha-color-primary-50: "#76d1f7"
  ha-color-primary-60: "#91daf8"
  ha-color-primary-70: "#ace4fa"
  ha-color-primary-80: "#c8edfc"
  ha-color-primary-90: "#e4f6fd"
  ha-color-primary-95: "#f1fafe"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Azure":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#08161b"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#00C2FF"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00C2FF"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#001820"
  ha-color-primary-10: "#003040"
  ha-color-primary-20: "#006180"
  ha-color-primary-30: "#0092bf"
  ha-color-primary-40: "#00C2FF"
  ha-color-primary-50: "#2accff"
  ha-color-primary-60: "#55d6ff"
  ha-color-primary-70: "#80e0ff"
  ha-color-primary-80: "#aaebff"
  ha-color-primary-90: "#d4f5ff"
  ha-color-primary-95: "#eafaff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Cobalt":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a1319"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#0A84FF"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#0A84FF"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#011020"
  ha-color-primary-10: "#022140"
  ha-color-primary-20: "#054280"
  ha-color-primary-30: "#0863bf"
  ha-color-primary-40: "#0A84FF"
  ha-color-primary-50: "#3398ff"
  ha-color-primary-60: "#5cadff"
  ha-color-primary-70: "#84c2ff"
  ha-color-primary-80: "#add6ff"
  ha-color-primary-90: "#d6eaff"
  ha-color-primary-95: "#ebf5ff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Indigo":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#080c14"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#9B8CFF"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#9B8CFF"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#131220"
  ha-color-primary-10: "#272340"
  ha-color-primary-20: "#4e4680"
  ha-color-primary-30: "#7469bf"
  ha-color-primary-40: "#9B8CFF"
  ha-color-primary-50: "#ac9fff"
  ha-color-primary-60: "#bcb2ff"
  ha-color-primary-70: "#cdc6ff"
  ha-color-primary-80: "#ded9ff"
  ha-color-primary-90: "#eeecff"
  ha-color-primary-95: "#f7f5ff"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Violet":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0e0e14"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#BF5AF2"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#BF5AF2"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#180b1e"
  ha-color-primary-10: "#30163c"
  ha-color-primary-20: "#602d79"
  ha-color-primary-30: "#8f44b6"
  ha-color-primary-40: "#BF5AF2"
  ha-color-primary-50: "#ca76f4"
  ha-color-primary-60: "#d491f6"
  ha-color-primary-70: "#dfacf8"
  ha-color-primary-80: "#eac8fb"
  ha-color-primary-90: "#f4e4fd"
  ha-color-primary-95: "#faf1fe"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Sand":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#121010"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#B8A38A"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B8A38A"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#171411"
  ha-color-primary-10: "#2e2922"
  ha-color-primary-20: "#5c5245"
  ha-color-primary-30: "#8a7a68"
  ha-color-primary-40: "#B8A38A"
  ha-color-primary-50: "#c4b29e"
  ha-color-primary-60: "#d0c2b1"
  ha-color-primary-70: "#dcd1c4"
  ha-color-primary-80: "#e7e0d8"
  ha-color-primary-90: "#f3f0ec"
  ha-color-primary-95: "#f9f7f5"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberprep - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberprep - Graphite":
  modes:
    light:
      primary-background-color: "rgb(70, 74, 80)"
      secondary-background-color: "rgb(70, 74, 80)"
      ha-card-background: "rgba(190, 195, 205, 0.26)"

    dark:
      primary-background-color: "rgb(14, 14, 20)"
      secondary-background-color: "rgb(14, 14, 20)"
      ha-card-background: "rgba(0, 0, 0, 0.26)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberprep/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0e1418"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(16px) saturate(1.45)"
  ha-card-border-radius: 30px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 0px
  ha-card-border-color: "rgba(255, 255, 255, 0.18)"
  ha-card-glass-tint: "rgba(255, 255, 255, 0.06)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(255,255,255,0.22) 0%, rgba(255,255,255,0.06) 22%, rgba(255,255,255,0) 45%)"
  ha-card-glass-inset-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ha-card-box-shadow: >
    3px 3px 0.5px -3.5px rgba(255,255,255,0.35) inset, -2px -2px 0.5px -2px rgba(255,255,255,0.30) inset, 0 0 10px 1px rgba(255,255,255,0.10) inset, 0 8px 24px -12px rgba(0,0,0,0.55)
  ultimate-glow-color: "#98989D"
  ultimate-transition: box-shadow 220ms ease, transform 220ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#98989D"
  accent-color: var(--primary-color)
  light-primary-color: "rgb(241, 241, 241)"
  divider-color: "rgba(152, 152, 157, 0.28)"
  ha-color-primary-05: "#131314"
  ha-color-primary-10: "#262627"
  ha-color-primary-20: "#4c4c4e"
  ha-color-primary-30: "#727276"
  ha-color-primary-40: "#98989D"
  ha-color-primary-50: "#a9a9ad"
  ha-color-primary-60: "#bababe"
  ha-color-primary-70: "#ccccce"
  ha-color-primary-80: "#ddddde"
  ha-color-primary-90: "#eeeeef"
  ha-color-primary-95: "#f6f6f7"

  # Apple / semantic palette
  red-color: "#FF453A"
  pink-color: "#FF375F"
  purple-color: "#BF5AF2"
  indigo-color: "#5E5CE6"
  blue-color: "#0A84FF"
  light-blue-color: "#66D4CF"
  cyan-color: "#5AC8F5"
  teal-color: "#6AC4DC"
  green-color: "#32D74B"
  yellow-color: "#FFD60A"
  orange-color: "#FF9F0A"
  brown-color: "#AC8E68"
  grey-color: "#8E8E93"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(255, 255, 255, 0.96)"
  secondary-text-color: "rgba(228, 228, 232, 0.78)"
  disabled-text-color: "rgba(208, 208, 208, 0.45)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(18, 18, 24, 0.55)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(255, 255, 255, 0.10)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(18, 18, 24, 0.30)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.35)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(32, 32, 40, 0.88)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(32, 32, 40, 0.88)"
  rgb-card-background-color: "rgb(20, 20, 26)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(32, 32, 40, 0.88)"
  material-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-background-color: "rgba(32, 32, 40, 0.88)"
  table-row-alternative-background-color: "rgba(44, 44, 54, 0.88)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 38, 0.72)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 38, 0.72)"
  more-info-header-background: "rgba(18, 18, 24, 0.30)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.55)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(255, 255, 255, 0.22)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 24px
  label-badge-background-color: "rgba(32, 32, 40, 0.88)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.45)"
  code-editor-background-color: "rgba(0, 0, 0, 0.45)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 30px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(255, 255, 255, 0.12)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(255, 255, 255, 0.12)"
  ha-color-fill-primary-quiet-hover: "rgba(255, 255, 255, 0.20)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 12px
  mush-icon-border-radius: 24px
  mush-control-border-radius: 24px
  mush-chip-border-radius: 24px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 24px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberprep - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 10, 132, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 191, 90, 242;
      --token-rgb-pink: 255, 55, 95;
      --token-rgb-red: 255, 69, 58;
      --token-rgb-deep-purple: 191, 90, 242;
      --token-rgb-indigo: 94, 92, 230;
      --token-rgb-blue: 10, 132, 255;
      --token-rgb-light-blue: 102, 212, 207;
      --token-rgb-cyan: 90, 200, 245;
      --token-rgb-teal: 106, 196, 220;
      --token-rgb-green: 50, 215, 75;
      --token-rgb-light-green: 50, 215, 75;
      --token-rgb-lime: 50, 215, 75;
      --token-rgb-yellow: 255, 214, 10;
      --token-rgb-amber: 255, 214, 10;
      --token-rgb-orange: 255, 159, 10;
      --token-rgb-deep-orange: 255, 159, 10;
      --token-rgb-brown: 172, 142, 104;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.22);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 0px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 24px -6px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(40, 42, 52, 0.86);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(0,0,0,0.12) 0%, rgba(0,0,0,0.30) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #313244 0%, #1e1e2e 55%, #181825 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #313244 0%, #1e1e2e 55%, #181825 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#303f58"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#89b4fa"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#89b4fa"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#11161f"
  ha-color-primary-10: "#222d3e"
  ha-color-primary-20: "#445a7d"
  ha-color-primary-30: "#6787bc"
  ha-color-primary-40: "#89b4fa"
  ha-color-primary-50: "#9dc0fb"
  ha-color-primary-60: "#b0cdfc"
  ha-color-primary-70: "#c4dafc"
  ha-color-primary-80: "#d8e6fd"
  ha-color-primary-90: "#ebf2fe"
  ha-color-primary-95: "#f5f9ff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Ember":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#12110b"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F2CDCD"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F2CDCD"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1e1a1a"
  ha-color-primary-10: "#3c3333"
  ha-color-primary-20: "#796666"
  ha-color-primary-30: "#b69a9a"
  ha-color-primary-40: "#F2CDCD"
  ha-color-primary-50: "#f4d5d5"
  ha-color-primary-60: "#f6dede"
  ha-color-primary-70: "#f8e6e6"
  ha-color-primary-80: "#fbeeee"
  ha-color-primary-90: "#fdf7f7"
  ha-color-primary-95: "#fefbfb"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Rose":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0e1414"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#EBA0AC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#EBA0AC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1d1416"
  ha-color-primary-10: "#3b282b"
  ha-color-primary-20: "#765056"
  ha-color-primary-30: "#b07881"
  ha-color-primary-40: "#EBA0AC"
  ha-color-primary-50: "#eeb0ba"
  ha-color-primary-60: "#f2c0c8"
  ha-color-primary-70: "#f5d0d6"
  ha-color-primary-80: "#f8dfe3"
  ha-color-primary-90: "#fceff1"
  ha-color-primary-95: "#fdf7f8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Amber":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0b160f"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Citrine":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1d1c09"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Lime":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0f1a0b"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Verdant":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a170c"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#A6E3A1"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#A6E3A1"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#151c14"
  ha-color-primary-10: "#2a3928"
  ha-color-primary-20: "#537250"
  ha-color-primary-30: "#7caa79"
  ha-color-primary-40: "#A6E3A1"
  ha-color-primary-50: "#b5e8b1"
  ha-color-primary-60: "#c4ecc0"
  ha-color-primary-70: "#d2f1d0"
  ha-color-primary-80: "#e1f6e0"
  ha-color-primary-90: "#f0faef"
  ha-color-primary-95: "#f8fdf7"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Jade":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0c1b12"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#94E2D5"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#94E2D5"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#121c1b"
  ha-color-primary-10: "#253835"
  ha-color-primary-20: "#4a716a"
  ha-color-primary-30: "#6faaa0"
  ha-color-primary-40: "#94E2D5"
  ha-color-primary-50: "#a6e7dc"
  ha-color-primary-60: "#b8ece3"
  ha-color-primary-70: "#caf0ea"
  ha-color-primary-80: "#dbf5f1"
  ha-color-primary-90: "#edfaf8"
  ha-color-primary-95: "#f6fdfc"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Lagoon":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#071e1c"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#74C7EC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#74C7EC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#0e191e"
  ha-color-primary-10: "#1d323b"
  ha-color-primary-20: "#3a6476"
  ha-color-primary-30: "#5795b1"
  ha-color-primary-40: "#74C7EC"
  ha-color-primary-50: "#8bd0ef"
  ha-color-primary-60: "#a2daf2"
  ha-color-primary-70: "#bae3f6"
  ha-color-primary-80: "#d1ecf9"
  ha-color-primary-90: "#e8f6fc"
  ha-color-primary-95: "#f3fafd"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Azure":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#081b17"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#74C7EC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#74C7EC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#0e191e"
  ha-color-primary-10: "#1d323b"
  ha-color-primary-20: "#3a6476"
  ha-color-primary-30: "#5795b1"
  ha-color-primary-40: "#74C7EC"
  ha-color-primary-50: "#8bd0ef"
  ha-color-primary-60: "#a2daf2"
  ha-color-primary-70: "#bae3f6"
  ha-color-primary-80: "#d1ecf9"
  ha-color-primary-90: "#e8f6fc"
  ha-color-primary-95: "#f3fafd"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Cobalt":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0e1b17"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#89B4FA"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#89B4FA"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#11161f"
  ha-color-primary-10: "#222d3e"
  ha-color-primary-20: "#445a7d"
  ha-color-primary-30: "#6787bc"
  ha-color-primary-40: "#89B4FA"
  ha-color-primary-50: "#9dc0fb"
  ha-color-primary-60: "#b0cdfc"
  ha-color-primary-70: "#c4dafc"
  ha-color-primary-80: "#d8e6fd"
  ha-color-primary-90: "#ebf2fe"
  ha-color-primary-95: "#f5f9ff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Indigo":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#081211"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#B4BEFE"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4BEFE"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#161820"
  ha-color-primary-10: "#2d3040"
  ha-color-primary-20: "#5a5f7f"
  ha-color-primary-30: "#878ebe"
  ha-color-primary-40: "#B4BEFE"
  ha-color-primary-50: "#c0c9fe"
  ha-color-primary-60: "#cdd4fe"
  ha-color-primary-70: "#dadefe"
  ha-color-primary-80: "#e6e9ff"
  ha-color-primary-90: "#f2f4ff"
  ha-color-primary-95: "#f9faff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Violet":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#161515"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#CBA6F7"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#CBA6F7"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#19151f"
  ha-color-primary-10: "#332a3e"
  ha-color-primary-20: "#66537c"
  ha-color-primary-30: "#987cb9"
  ha-color-primary-40: "#CBA6F7"
  ha-color-primary-50: "#d4b5f8"
  ha-color-primary-60: "#dcc4fa"
  ha-color-primary-70: "#e5d2fb"
  ha-color-primary-80: "#eee1fc"
  ha-color-primary-90: "#f6f0fe"
  ha-color-primary-95: "#fbf8fe"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Sand":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#101b10"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Solarpunk - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Solarpunk - Graphite":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/solarpunk/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#121b15"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#B4BEFE"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4BEFE"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#161820"
  ha-color-primary-10: "#2d3040"
  ha-color-primary-20: "#5a5f7f"
  ha-color-primary-30: "#878ebe"
  ha-color-primary-40: "#B4BEFE"
  ha-color-primary-50: "#c0c9fe"
  ha-color-primary-60: "#cdd4fe"
  ha-color-primary-70: "#dadefe"
  ha-color-primary-80: "#e6e9ff"
  ha-color-primary-90: "#f2f4ff"
  ha-color-primary-95: "#f9faff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Solarpunk - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #313244 0%, #1e1e2e 55%, #181825 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #313244 0%, #1e1e2e 55%, #181825 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#303f58"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#89b4fa"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#89b4fa"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#11161f"
  ha-color-primary-10: "#222d3e"
  ha-color-primary-20: "#445a7d"
  ha-color-primary-30: "#6787bc"
  ha-color-primary-40: "#89b4fa"
  ha-color-primary-50: "#9dc0fb"
  ha-color-primary-60: "#b0cdfc"
  ha-color-primary-70: "#c4dafc"
  ha-color-primary-80: "#d8e6fd"
  ha-color-primary-90: "#ebf2fe"
  ha-color-primary-95: "#f5f9ff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Ember":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#160704"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F2CDCD"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F2CDCD"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1e1a1a"
  ha-color-primary-10: "#3c3333"
  ha-color-primary-20: "#796666"
  ha-color-primary-30: "#b69a9a"
  ha-color-primary-40: "#F2CDCD"
  ha-color-primary-50: "#f4d5d5"
  ha-color-primary-60: "#f6dede"
  ha-color-primary-70: "#f8e6e6"
  ha-color-primary-80: "#fbeeee"
  ha-color-primary-90: "#fdf7f7"
  ha-color-primary-95: "#fefbfb"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Rose":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#140a14"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#EBA0AC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#EBA0AC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1d1416"
  ha-color-primary-10: "#3b282b"
  ha-color-primary-20: "#765056"
  ha-color-primary-30: "#b07881"
  ha-color-primary-40: "#EBA0AC"
  ha-color-primary-50: "#eeb0ba"
  ha-color-primary-60: "#f2c0c8"
  ha-color-primary-70: "#f5d0d6"
  ha-color-primary-80: "#f8dfe3"
  ha-color-primary-90: "#fceff1"
  ha-color-primary-95: "#fdf7f8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Amber":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#140e08"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Citrine":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#190c03"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Lime":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#110e05"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Verdant":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#101208"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#A6E3A1"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#A6E3A1"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#151c14"
  ha-color-primary-10: "#2a3928"
  ha-color-primary-20: "#537250"
  ha-color-primary-30: "#7caa79"
  ha-color-primary-40: "#A6E3A1"
  ha-color-primary-50: "#b5e8b1"
  ha-color-primary-60: "#c4ecc0"
  ha-color-primary-70: "#d2f1d0"
  ha-color-primary-80: "#e1f6e0"
  ha-color-primary-90: "#f0faef"
  ha-color-primary-95: "#f8fdf7"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Jade":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0d0b09"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#94E2D5"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#94E2D5"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#121c1b"
  ha-color-primary-10: "#253835"
  ha-color-primary-20: "#4a716a"
  ha-color-primary-30: "#6faaa0"
  ha-color-primary-40: "#94E2D5"
  ha-color-primary-50: "#a6e7dc"
  ha-color-primary-60: "#b8ece3"
  ha-color-primary-70: "#caf0ea"
  ha-color-primary-80: "#dbf5f1"
  ha-color-primary-90: "#edfaf8"
  ha-color-primary-95: "#f6fdfc"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Lagoon":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0d1414"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#74C7EC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#74C7EC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#0e191e"
  ha-color-primary-10: "#1d323b"
  ha-color-primary-20: "#3a6476"
  ha-color-primary-30: "#5795b1"
  ha-color-primary-40: "#74C7EC"
  ha-color-primary-50: "#8bd0ef"
  ha-color-primary-60: "#a2daf2"
  ha-color-primary-70: "#bae3f6"
  ha-color-primary-80: "#d1ecf9"
  ha-color-primary-90: "#e8f6fc"
  ha-color-primary-95: "#f3fafd"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Azure":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a1011"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#74C7EC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#74C7EC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#0e191e"
  ha-color-primary-10: "#1d323b"
  ha-color-primary-20: "#3a6476"
  ha-color-primary-30: "#5795b1"
  ha-color-primary-40: "#74C7EC"
  ha-color-primary-50: "#8bd0ef"
  ha-color-primary-60: "#a2daf2"
  ha-color-primary-70: "#bae3f6"
  ha-color-primary-80: "#d1ecf9"
  ha-color-primary-90: "#e8f6fc"
  ha-color-primary-95: "#f3fafd"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Cobalt":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#131314"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#89B4FA"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#89B4FA"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#11161f"
  ha-color-primary-10: "#222d3e"
  ha-color-primary-20: "#445a7d"
  ha-color-primary-30: "#6787bc"
  ha-color-primary-40: "#89B4FA"
  ha-color-primary-50: "#9dc0fb"
  ha-color-primary-60: "#b0cdfc"
  ha-color-primary-70: "#c4dafc"
  ha-color-primary-80: "#d8e6fd"
  ha-color-primary-90: "#ebf2fe"
  ha-color-primary-95: "#f5f9ff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Indigo":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#100a0a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#B4BEFE"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4BEFE"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#161820"
  ha-color-primary-10: "#2d3040"
  ha-color-primary-20: "#5a5f7f"
  ha-color-primary-30: "#878ebe"
  ha-color-primary-40: "#B4BEFE"
  ha-color-primary-50: "#c0c9fe"
  ha-color-primary-60: "#cdd4fe"
  ha-color-primary-70: "#dadefe"
  ha-color-primary-80: "#e6e9ff"
  ha-color-primary-90: "#f2f4ff"
  ha-color-primary-95: "#f9faff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Violet":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#13080e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#CBA6F7"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#CBA6F7"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#19151f"
  ha-color-primary-10: "#332a3e"
  ha-color-primary-20: "#66537c"
  ha-color-primary-30: "#987cb9"
  ha-color-primary-40: "#CBA6F7"
  ha-color-primary-50: "#d4b5f8"
  ha-color-primary-60: "#dcc4fa"
  ha-color-primary-70: "#e5d2fb"
  ha-color-primary-80: "#eee1fc"
  ha-color-primary-90: "#f6f0fe"
  ha-color-primary-95: "#fbf8fe"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Sand":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#100a08"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Dark Academia - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Dark Academia - Graphite":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/dark-academia/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#100d0b"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#B4BEFE"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4BEFE"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#161820"
  ha-color-primary-10: "#2d3040"
  ha-color-primary-20: "#5a5f7f"
  ha-color-primary-30: "#878ebe"
  ha-color-primary-40: "#B4BEFE"
  ha-color-primary-50: "#c0c9fe"
  ha-color-primary-60: "#cdd4fe"
  ha-color-primary-70: "#dadefe"
  ha-color-primary-80: "#e6e9ff"
  ha-color-primary-90: "#f2f4ff"
  ha-color-primary-95: "#f9faff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Dark Academia - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #313244 0%, #1e1e2e 55%, #181825 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #313244 0%, #1e1e2e 55%, #181825 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#303f58"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#89b4fa"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#89b4fa"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#11161f"
  ha-color-primary-10: "#222d3e"
  ha-color-primary-20: "#445a7d"
  ha-color-primary-30: "#6787bc"
  ha-color-primary-40: "#89b4fa"
  ha-color-primary-50: "#9dc0fb"
  ha-color-primary-60: "#b0cdfc"
  ha-color-primary-70: "#c4dafc"
  ha-color-primary-80: "#d8e6fd"
  ha-color-primary-90: "#ebf2fe"
  ha-color-primary-95: "#f5f9ff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Ember":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#17110a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F2CDCD"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F2CDCD"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1e1a1a"
  ha-color-primary-10: "#3c3333"
  ha-color-primary-20: "#796666"
  ha-color-primary-30: "#b69a9a"
  ha-color-primary-40: "#F2CDCD"
  ha-color-primary-50: "#f4d5d5"
  ha-color-primary-60: "#f6dede"
  ha-color-primary-70: "#f8e6e6"
  ha-color-primary-80: "#fbeeee"
  ha-color-primary-90: "#fdf7f7"
  ha-color-primary-95: "#fefbfb"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Rose":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#141411"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#EBA0AC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#EBA0AC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1d1416"
  ha-color-primary-10: "#3b282b"
  ha-color-primary-20: "#765056"
  ha-color-primary-30: "#b07881"
  ha-color-primary-40: "#EBA0AC"
  ha-color-primary-50: "#eeb0ba"
  ha-color-primary-60: "#f2c0c8"
  ha-color-primary-70: "#f5d0d6"
  ha-color-primary-80: "#f8dfe3"
  ha-color-primary-90: "#fceff1"
  ha-color-primary-95: "#fdf7f8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Amber":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1b1c0f"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Citrine":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1e1809"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Lime":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1a1e0e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Verdant":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#141c0e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#A6E3A1"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#A6E3A1"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#151c14"
  ha-color-primary-10: "#2a3928"
  ha-color-primary-20: "#537250"
  ha-color-primary-30: "#7caa79"
  ha-color-primary-40: "#A6E3A1"
  ha-color-primary-50: "#b5e8b1"
  ha-color-primary-60: "#c4ecc0"
  ha-color-primary-70: "#d2f1d0"
  ha-color-primary-80: "#e1f6e0"
  ha-color-primary-90: "#f0faef"
  ha-color-primary-95: "#f8fdf7"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Jade":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#151a10"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#94E2D5"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#94E2D5"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#121c1b"
  ha-color-primary-10: "#253835"
  ha-color-primary-20: "#4a716a"
  ha-color-primary-30: "#6faaa0"
  ha-color-primary-40: "#94E2D5"
  ha-color-primary-50: "#a6e7dc"
  ha-color-primary-60: "#b8ece3"
  ha-color-primary-70: "#caf0ea"
  ha-color-primary-80: "#dbf5f1"
  ha-color-primary-90: "#edfaf8"
  ha-color-primary-95: "#f6fdfc"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Lagoon":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#111d17"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#74C7EC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#74C7EC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#0e191e"
  ha-color-primary-10: "#1d323b"
  ha-color-primary-20: "#3a6476"
  ha-color-primary-30: "#5795b1"
  ha-color-primary-40: "#74C7EC"
  ha-color-primary-50: "#8bd0ef"
  ha-color-primary-60: "#a2daf2"
  ha-color-primary-70: "#bae3f6"
  ha-color-primary-80: "#d1ecf9"
  ha-color-primary-90: "#e8f6fc"
  ha-color-primary-95: "#f3fafd"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Azure":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0e1a15"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#74C7EC"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#74C7EC"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#0e191e"
  ha-color-primary-10: "#1d323b"
  ha-color-primary-20: "#3a6476"
  ha-color-primary-30: "#5795b1"
  ha-color-primary-40: "#74C7EC"
  ha-color-primary-50: "#8bd0ef"
  ha-color-primary-60: "#a2daf2"
  ha-color-primary-70: "#bae3f6"
  ha-color-primary-80: "#d1ecf9"
  ha-color-primary-90: "#e8f6fc"
  ha-color-primary-95: "#f3fafd"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Cobalt":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#141c18"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#89B4FA"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#89B4FA"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#11161f"
  ha-color-primary-10: "#222d3e"
  ha-color-primary-20: "#445a7d"
  ha-color-primary-30: "#6787bc"
  ha-color-primary-40: "#89B4FA"
  ha-color-primary-50: "#9dc0fb"
  ha-color-primary-60: "#b0cdfc"
  ha-color-primary-70: "#c4dafc"
  ha-color-primary-80: "#d8e6fd"
  ha-color-primary-90: "#ebf2fe"
  ha-color-primary-95: "#f5f9ff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Indigo":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#101411"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#B4BEFE"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4BEFE"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#161820"
  ha-color-primary-10: "#2d3040"
  ha-color-primary-20: "#5a5f7f"
  ha-color-primary-30: "#878ebe"
  ha-color-primary-40: "#B4BEFE"
  ha-color-primary-50: "#c0c9fe"
  ha-color-primary-60: "#cdd4fe"
  ha-color-primary-70: "#dadefe"
  ha-color-primary-80: "#e6e9ff"
  ha-color-primary-90: "#f2f4ff"
  ha-color-primary-95: "#f9faff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Violet":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#1c1316"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#CBA6F7"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#CBA6F7"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#19151f"
  ha-color-primary-10: "#332a3e"
  ha-color-primary-20: "#66537c"
  ha-color-primary-30: "#987cb9"
  ha-color-primary-40: "#CBA6F7"
  ha-color-primary-50: "#d4b5f8"
  ha-color-primary-60: "#dcc4fa"
  ha-color-primary-70: "#e5d2fb"
  ha-color-primary-80: "#eee1fc"
  ha-color-primary-90: "#f6f0fe"
  ha-color-primary-95: "#fbf8fe"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Sand":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#15160e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#F9E2AF"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#F9E2AF"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#1f1c16"
  ha-color-primary-10: "#3e382c"
  ha-color-primary-20: "#7c7158"
  ha-color-primary-30: "#bbaa83"
  ha-color-primary-40: "#F9E2AF"
  ha-color-primary-50: "#fae7bc"
  ha-color-primary-60: "#fbecca"
  ha-color-primary-70: "#fcf0d7"
  ha-color-primary-80: "#fdf5e4"
  ha-color-primary-90: "#fefaf2"
  ha-color-primary-95: "#fefdf8"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cottagecore - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cottagecore - Graphite":
  modes:
    light:
      primary-background-color: "#585b70"
      secondary-background-color: "#585b70"
      ha-card-background: "rgba(88, 91, 112, 0.42)"

    dark:
      primary-background-color: "#1e1e2e"
      secondary-background-color: "#181825"
      ha-card-background: "rgba(30, 30, 46, 0.42)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cottagecore/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#181a11"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(12px) saturate(1.15)"
  ha-card-border-radius: 18px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(205, 214, 244, 0.10)"
  ha-card-glass-tint: "rgba(49, 50, 68, 0.42)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(205,214,244,0.12) 0%, rgba(205,214,244,0.03) 26%, rgba(205,214,244,0) 50%)"
  ha-card-glass-inset-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ha-card-box-shadow: >
    0 1px 0 0 rgba(205,214,244,0.10) inset, 0 0 0 1px rgba(17,17,27,0.35), 0 10px 26px -14px rgba(0,0,0,0.70)
  ultimate-glow-color: "#B4BEFE"
  ultimate-transition: box-shadow 200ms ease, transform 200ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B4BEFE"
  accent-color: var(--primary-color)
  light-primary-color: "#cdd6f4"
  divider-color: "rgba(147, 153, 178, 0.24)"
  ha-color-primary-05: "#161820"
  ha-color-primary-10: "#2d3040"
  ha-color-primary-20: "#5a5f7f"
  ha-color-primary-30: "#878ebe"
  ha-color-primary-40: "#B4BEFE"
  ha-color-primary-50: "#c0c9fe"
  ha-color-primary-60: "#cdd4fe"
  ha-color-primary-70: "#dadefe"
  ha-color-primary-80: "#e6e9ff"
  ha-color-primary-90: "#f2f4ff"
  ha-color-primary-95: "#f9faff"

  # Apple / semantic palette
  red-color: "#f38ba8"
  pink-color: "#f5c2e7"
  purple-color: "#cba6f7"
  indigo-color: "#b4befe"
  blue-color: "#89b4fa"
  light-blue-color: "#89dceb"
  cyan-color: "#94e2d5"
  teal-color: "#94e2d5"
  green-color: "#a6e3a1"
  yellow-color: "#f9e2af"
  orange-color: "#fab387"
  brown-color: "#dc8a78"
  grey-color: "#9399b2"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "#cdd6f4"
  secondary-text-color: "#a6adc8"
  disabled-text-color: "rgba(127, 132, 156, 0.65)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(24, 24, 37, 0.62)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "rgba(49, 50, 68, 0.85)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(24, 24, 37, 0.38)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(49, 50, 68, 0.92)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(49, 50, 68, 0.92)"
  rgb-card-background-color: "rgb(30, 30, 46)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(49, 50, 68, 0.92)"
  material-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-background-color: "rgba(49, 50, 68, 0.92)"
  table-row-alternative-background-color: "rgba(69, 71, 90, 0.92)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(30, 30, 46, 0.80)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(30, 30, 46, 0.80)"
  more-info-header-background: "rgba(24, 24, 37, 0.38)"
  mdc-dialog-scrim-color: "rgba(17, 17, 27, 0.62)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(205, 214, 244, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 14px
  label-badge-background-color: "rgba(49, 50, 68, 0.92)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(17, 17, 27, 0.70)"
  code-editor-background-color: "rgba(17, 17, 27, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 18px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "rgba(205, 214, 244, 0.10)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "rgba(205, 214, 244, 0.10)"
  ha-color-fill-primary-quiet-hover: "rgba(205, 214, 244, 0.18)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 10px
  mush-icon-border-radius: 14px
  mush-control-border-radius: 14px
  mush-chip-border-radius: 14px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 14px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cottagecore - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 137, 180, 250;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 203, 166, 247;
      --token-rgb-pink: 245, 194, 231;
      --token-rgb-red: 243, 139, 168;
      --token-rgb-deep-purple: 203, 166, 247;
      --token-rgb-indigo: 180, 190, 254;
      --token-rgb-blue: 137, 180, 250;
      --token-rgb-light-blue: 137, 220, 235;
      --token-rgb-cyan: 148, 226, 213;
      --token-rgb-teal: 148, 226, 213;
      --token-rgb-green: 166, 227, 161;
      --token-rgb-light-green: 166, 227, 161;
      --token-rgb-lime: 166, 227, 161;
      --token-rgb-yellow: 249, 226, 175;
      --token-rgb-amber: 249, 226, 175;
      --token-rgb-orange: 250, 179, 135;
      --token-rgb-deep-orange: 250, 179, 135;
      --token-rgb-brown: 220, 138, 120;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: rgba(147, 153, 178, 0.35);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: normal;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 20px -8px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(49, 50, 68, 0.92);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: linear-gradient(180deg, rgba(17,17,27,0.28) 0%, rgba(17,17,27,0.48) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #12002b 0%, #04050a 55%, #001a2b 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #12002b 0%, #04050a 55%, #001a2b 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#003959"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00A3FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00A3FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001420"
  ha-color-primary-10: "#002940"
  ha-color-primary-20: "#005280"
  ha-color-primary-30: "#007abf"
  ha-color-primary-40: "#00A3FF"
  ha-color-primary-50: "#2ab2ff"
  ha-color-primary-60: "#55c2ff"
  ha-color-primary-70: "#80d1ff"
  ha-color-primary-80: "#aae0ff"
  ha-color-primary-90: "#d4f0ff"
  ha-color-primary-95: "#eaf7ff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Ember":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#13080b"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF0E00"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF0E00"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#200200"
  ha-color-primary-10: "#400400"
  ha-color-primary-20: "#800700"
  ha-color-primary-30: "#bf0a00"
  ha-color-primary-40: "#FF0E00"
  ha-color-primary-50: "#ff362a"
  ha-color-primary-60: "#ff5e55"
  ha-color-primary-70: "#ff8680"
  ha-color-primary-80: "#ffafaa"
  ha-color-primary-90: "#ffd7d4"
  ha-color-primary-95: "#ffebea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Rose":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a0c15"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF0033"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF0033"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#200006"
  ha-color-primary-10: "#40000d"
  ha-color-primary-20: "#80001a"
  ha-color-primary-30: "#bf0026"
  ha-color-primary-40: "#FF0033"
  ha-color-primary-50: "#ff2a55"
  ha-color-primary-60: "#ff5577"
  ha-color-primary-70: "#ff8099"
  ha-color-primary-80: "#ffaabb"
  ha-color-primary-90: "#ffd4dd"
  ha-color-primary-95: "#ffeaee"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Amber":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0e0f16"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF9B00"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF9B00"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201300"
  ha-color-primary-10: "#402700"
  ha-color-primary-20: "#804e00"
  ha-color-primary-30: "#bf7400"
  ha-color-primary-40: "#FF9B00"
  ha-color-primary-50: "#ffac2a"
  ha-color-primary-60: "#ffbc55"
  ha-color-primary-70: "#ffcd80"
  ha-color-primary-80: "#ffdeaa"
  ha-color-primary-90: "#ffeed4"
  ha-color-primary-95: "#fff7ea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Citrine":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#14100a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FFD400"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFD400"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201a00"
  ha-color-primary-10: "#403500"
  ha-color-primary-20: "#806a00"
  ha-color-primary-30: "#bf9f00"
  ha-color-primary-40: "#FFD400"
  ha-color-primary-50: "#ffdb2a"
  ha-color-primary-60: "#ffe255"
  ha-color-primary-70: "#ffea80"
  ha-color-primary-80: "#fff1aa"
  ha-color-primary-90: "#fff8d4"
  ha-color-primary-95: "#fffbea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Lime":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a160d"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#B8FF03"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B8FF03"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#172000"
  ha-color-primary-10: "#2e4001"
  ha-color-primary-20: "#5c8002"
  ha-color-primary-30: "#8abf02"
  ha-color-primary-40: "#B8FF03"
  ha-color-primary-50: "#c4ff2d"
  ha-color-primary-60: "#d0ff57"
  ha-color-primary-70: "#dcff81"
  ha-color-primary-80: "#e7ffab"
  ha-color-primary-90: "#f3ffd5"
  ha-color-primary-95: "#f9ffea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Verdant":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#071110"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00FF27"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00FF27"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#002005"
  ha-color-primary-10: "#00400a"
  ha-color-primary-20: "#008014"
  ha-color-primary-30: "#00bf1d"
  ha-color-primary-40: "#00FF27"
  ha-color-primary-50: "#2aff4b"
  ha-color-primary-60: "#55ff6f"
  ha-color-primary-70: "#80ff93"
  ha-color-primary-80: "#aaffb7"
  ha-color-primary-90: "#d4ffdb"
  ha-color-primary-95: "#eaffed"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Jade":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0c0d0b"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#38FFC7"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#38FFC7"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#072019"
  ha-color-primary-10: "#0e4032"
  ha-color-primary-20: "#1c8064"
  ha-color-primary-30: "#2abf95"
  ha-color-primary-40: "#38FFC7"
  ha-color-primary-50: "#59ffd0"
  ha-color-primary-60: "#7affda"
  ha-color-primary-70: "#9cffe3"
  ha-color-primary-80: "#bdffec"
  ha-color-primary-90: "#defff6"
  ha-color-primary-95: "#eefffa"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Lagoon":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#08141a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#25C0FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#25C0FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#051820"
  ha-color-primary-10: "#093040"
  ha-color-primary-20: "#126080"
  ha-color-primary-30: "#1c90bf"
  ha-color-primary-40: "#25C0FF"
  ha-color-primary-50: "#49caff"
  ha-color-primary-60: "#6ed5ff"
  ha-color-primary-70: "#92e0ff"
  ha-color-primary-80: "#b6eaff"
  ha-color-primary-90: "#dbf4ff"
  ha-color-primary-95: "#edfaff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Azure":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#07161b"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00C2FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00C2FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001820"
  ha-color-primary-10: "#003040"
  ha-color-primary-20: "#006180"
  ha-color-primary-30: "#0092bf"
  ha-color-primary-40: "#00C2FF"
  ha-color-primary-50: "#2accff"
  ha-color-primary-60: "#55d6ff"
  ha-color-primary-70: "#80e0ff"
  ha-color-primary-80: "#aaebff"
  ha-color-primary-90: "#d4f5ff"
  ha-color-primary-95: "#eafaff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Cobalt":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0d1014"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#007FFF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#007FFF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001020"
  ha-color-primary-10: "#002040"
  ha-color-primary-20: "#004080"
  ha-color-primary-30: "#005fbf"
  ha-color-primary-40: "#007FFF"
  ha-color-primary-50: "#2a94ff"
  ha-color-primary-60: "#55aaff"
  ha-color-primary-70: "#80bfff"
  ha-color-primary-80: "#aad4ff"
  ha-color-primary-90: "#d4eaff"
  ha-color-primary-95: "#eaf4ff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Indigo":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0b0910"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#5238FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#5238FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#0a0720"
  ha-color-primary-10: "#140e40"
  ha-color-primary-20: "#291c80"
  ha-color-primary-30: "#3e2abf"
  ha-color-primary-40: "#5238FF"
  ha-color-primary-50: "#6f59ff"
  ha-color-primary-60: "#8c7aff"
  ha-color-primary-70: "#a89cff"
  ha-color-primary-80: "#c5bdff"
  ha-color-primary-90: "#e2deff"
  ha-color-primary-95: "#f1eeff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Violet":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0f0915"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#B627FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B627FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#170520"
  ha-color-primary-10: "#2e0a40"
  ha-color-primary-20: "#5b1480"
  ha-color-primary-30: "#881dbf"
  ha-color-primary-40: "#B627FF"
  ha-color-primary-50: "#c24bff"
  ha-color-primary-60: "#ce6fff"
  ha-color-primary-70: "#da93ff"
  ha-color-primary-80: "#e7b7ff"
  ha-color-primary-90: "#f3dbff"
  ha-color-primary-95: "#f9edff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Sand":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0c090c"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FFA438"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFA438"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201407"
  ha-color-primary-10: "#40290e"
  ha-color-primary-20: "#80521c"
  ha-color-primary-30: "#bf7b2a"
  ha-color-primary-40: "#FFA438"
  ha-color-primary-50: "#ffb359"
  ha-color-primary-60: "#ffc27a"
  ha-color-primary-70: "#ffd29c"
  ha-color-primary-80: "#ffe1bd"
  ha-color-primary-90: "#fff0de"
  ha-color-primary-95: "#fff7ee"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Cyberpunk - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Cyberpunk - Graphite":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/cyberpunk/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#081013"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#3838FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#3838FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#070720"
  ha-color-primary-10: "#0e0e40"
  ha-color-primary-20: "#1c1c80"
  ha-color-primary-30: "#2a2abf"
  ha-color-primary-40: "#3838FF"
  ha-color-primary-50: "#5959ff"
  ha-color-primary-60: "#7a7aff"
  ha-color-primary-70: "#9c9cff"
  ha-color-primary-80: "#bdbdff"
  ha-color-primary-90: "#dedeff"
  ha-color-primary-95: "#eeeeff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Cyberpunk - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #12002b 0%, #04050a 55%, #001a2b 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #12002b 0%, #04050a 55%, #001a2b 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#003959"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00A3FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00A3FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001420"
  ha-color-primary-10: "#002940"
  ha-color-primary-20: "#005280"
  ha-color-primary-30: "#007abf"
  ha-color-primary-40: "#00A3FF"
  ha-color-primary-50: "#2ab2ff"
  ha-color-primary-60: "#55c2ff"
  ha-color-primary-70: "#80d1ff"
  ha-color-primary-80: "#aae0ff"
  ha-color-primary-90: "#d4f0ff"
  ha-color-primary-95: "#eaf7ff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Ember":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#120e28"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF0E00"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF0E00"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#200200"
  ha-color-primary-10: "#400400"
  ha-color-primary-20: "#800700"
  ha-color-primary-30: "#bf0a00"
  ha-color-primary-40: "#FF0E00"
  ha-color-primary-50: "#ff362a"
  ha-color-primary-60: "#ff5e55"
  ha-color-primary-70: "#ff8680"
  ha-color-primary-80: "#ffafaa"
  ha-color-primary-90: "#ffd7d4"
  ha-color-primary-95: "#ffebea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Rose":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#211328"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF0033"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF0033"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#200006"
  ha-color-primary-10: "#40000d"
  ha-color-primary-20: "#80001a"
  ha-color-primary-30: "#bf0026"
  ha-color-primary-40: "#FF0033"
  ha-color-primary-50: "#ff2a55"
  ha-color-primary-60: "#ff5577"
  ha-color-primary-70: "#ff8099"
  ha-color-primary-80: "#ffaabb"
  ha-color-primary-90: "#ffd4dd"
  ha-color-primary-95: "#ffeaee"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Amber":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#180c1e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF9B00"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF9B00"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201300"
  ha-color-primary-10: "#402700"
  ha-color-primary-20: "#804e00"
  ha-color-primary-30: "#bf7400"
  ha-color-primary-40: "#FF9B00"
  ha-color-primary-50: "#ffac2a"
  ha-color-primary-60: "#ffbc55"
  ha-color-primary-70: "#ffcd80"
  ha-color-primary-80: "#ffdeaa"
  ha-color-primary-90: "#ffeed4"
  ha-color-primary-95: "#fff7ea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Citrine":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#180f25"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FFD400"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFD400"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201a00"
  ha-color-primary-10: "#403500"
  ha-color-primary-20: "#806a00"
  ha-color-primary-30: "#bf9f00"
  ha-color-primary-40: "#FFD400"
  ha-color-primary-50: "#ffdb2a"
  ha-color-primary-60: "#ffe255"
  ha-color-primary-70: "#ffea80"
  ha-color-primary-80: "#fff1aa"
  ha-color-primary-90: "#fff8d4"
  ha-color-primary-95: "#fffbea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Lime":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#221420"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#B8FF03"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B8FF03"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#172000"
  ha-color-primary-10: "#2e4001"
  ha-color-primary-20: "#5c8002"
  ha-color-primary-30: "#8abf02"
  ha-color-primary-40: "#B8FF03"
  ha-color-primary-50: "#c4ff2d"
  ha-color-primary-60: "#d0ff57"
  ha-color-primary-70: "#dcff81"
  ha-color-primary-80: "#e7ffab"
  ha-color-primary-90: "#f3ffd5"
  ha-color-primary-95: "#f9ffea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Verdant":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#161022"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00FF27"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00FF27"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#002005"
  ha-color-primary-10: "#00400a"
  ha-color-primary-20: "#008014"
  ha-color-primary-30: "#00bf1d"
  ha-color-primary-40: "#00FF27"
  ha-color-primary-50: "#2aff4b"
  ha-color-primary-60: "#55ff6f"
  ha-color-primary-70: "#80ff93"
  ha-color-primary-80: "#aaffb7"
  ha-color-primary-90: "#d4ffdb"
  ha-color-primary-95: "#eaffed"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Jade":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0b0f23"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#38FFC7"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#38FFC7"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#072019"
  ha-color-primary-10: "#0e4032"
  ha-color-primary-20: "#1c8064"
  ha-color-primary-30: "#2abf95"
  ha-color-primary-40: "#38FFC7"
  ha-color-primary-50: "#59ffd0"
  ha-color-primary-60: "#7affda"
  ha-color-primary-70: "#9cffe3"
  ha-color-primary-80: "#bdffec"
  ha-color-primary-90: "#defff6"
  ha-color-primary-95: "#eefffa"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Lagoon":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0e0d20"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#25C0FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#25C0FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#051820"
  ha-color-primary-10: "#093040"
  ha-color-primary-20: "#126080"
  ha-color-primary-30: "#1c90bf"
  ha-color-primary-40: "#25C0FF"
  ha-color-primary-50: "#49caff"
  ha-color-primary-60: "#6ed5ff"
  ha-color-primary-70: "#92e0ff"
  ha-color-primary-80: "#b6eaff"
  ha-color-primary-90: "#dbf4ff"
  ha-color-primary-95: "#edfaff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Azure":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#14142c"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00C2FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00C2FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001820"
  ha-color-primary-10: "#003040"
  ha-color-primary-20: "#006180"
  ha-color-primary-30: "#0092bf"
  ha-color-primary-40: "#00C2FF"
  ha-color-primary-50: "#2accff"
  ha-color-primary-60: "#55d6ff"
  ha-color-primary-70: "#80e0ff"
  ha-color-primary-80: "#aaebff"
  ha-color-primary-90: "#d4f5ff"
  ha-color-primary-95: "#eafaff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Cobalt":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#161326"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#007FFF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#007FFF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001020"
  ha-color-primary-10: "#002040"
  ha-color-primary-20: "#004080"
  ha-color-primary-30: "#005fbf"
  ha-color-primary-40: "#007FFF"
  ha-color-primary-50: "#2a94ff"
  ha-color-primary-60: "#55aaff"
  ha-color-primary-70: "#80bfff"
  ha-color-primary-80: "#aad4ff"
  ha-color-primary-90: "#d4eaff"
  ha-color-primary-95: "#eaf4ff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Indigo":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#120d23"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#5238FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#5238FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#0a0720"
  ha-color-primary-10: "#140e40"
  ha-color-primary-20: "#291c80"
  ha-color-primary-30: "#3e2abf"
  ha-color-primary-40: "#5238FF"
  ha-color-primary-50: "#6f59ff"
  ha-color-primary-60: "#8c7aff"
  ha-color-primary-70: "#a89cff"
  ha-color-primary-80: "#c5bdff"
  ha-color-primary-90: "#e2deff"
  ha-color-primary-95: "#f1eeff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Violet":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#160420"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#B627FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B627FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#170520"
  ha-color-primary-10: "#2e0a40"
  ha-color-primary-20: "#5b1480"
  ha-color-primary-30: "#881dbf"
  ha-color-primary-40: "#B627FF"
  ha-color-primary-50: "#c24bff"
  ha-color-primary-60: "#ce6fff"
  ha-color-primary-70: "#da93ff"
  ha-color-primary-80: "#e7b7ff"
  ha-color-primary-90: "#f3dbff"
  ha-color-primary-95: "#f9edff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Sand":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#140b1c"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FFA438"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFA438"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201407"
  ha-color-primary-10: "#40290e"
  ha-color-primary-20: "#80521c"
  ha-color-primary-30: "#bf7b2a"
  ha-color-primary-40: "#FFA438"
  ha-color-primary-50: "#ffb359"
  ha-color-primary-60: "#ffc27a"
  ha-color-primary-70: "#ffd29c"
  ha-color-primary-80: "#ffe1bd"
  ha-color-primary-90: "#fff0de"
  ha-color-primary-95: "#fff7ee"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Synthwave - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Synthwave - Graphite":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/synthwave/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#100e29"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "color-mix(in srgb, var(--ultimate-glow-color) 45%, transparent)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(180deg, color-mix(in srgb, var(--ultimate-glow-color) 22%, transparent) 0%, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0) 40%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#3838FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#3838FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#070720"
  ha-color-primary-10: "#0e0e40"
  ha-color-primary-20: "#1c1c80"
  ha-color-primary-30: "#2a2abf"
  ha-color-primary-40: "#3838FF"
  ha-color-primary-50: "#5959ff"
  ha-color-primary-60: "#7a7aff"
  ha-color-primary-70: "#9c9cff"
  ha-color-primary-80: "#bdbdff"
  ha-color-primary-90: "#dedeff"
  ha-color-primary-95: "#eeeeff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Synthwave - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 0 30px -4px var(--ultimate-glow-color);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  
  ultimate-background: "linear-gradient(140deg, #12002b 0%, #04050a 55%, #001a2b 100%)"
  background-image: "center / cover no-repeat fixed linear-gradient(140deg, #12002b 0%, #04050a 55%, #001a2b 100%)"
  lovelace-background: var(--background-image)

  app-theme-color: "#003959"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00A3FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00A3FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001420"
  ha-color-primary-10: "#002940"
  ha-color-primary-20: "#005280"
  ha-color-primary-30: "#007abf"
  ha-color-primary-40: "#00A3FF"
  ha-color-primary-50: "#2ab2ff"
  ha-color-primary-60: "#55c2ff"
  ha-color-primary-70: "#80d1ff"
  ha-color-primary-80: "#aae0ff"
  ha-color-primary-90: "#d4f0ff"
  ha-color-primary-95: "#eaf7ff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Ember
# Area: Ember  (mdi:fire)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Ember":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/ember.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/ember.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#13080c"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF0E00"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF0E00"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#200200"
  ha-color-primary-10: "#400400"
  ha-color-primary-20: "#800700"
  ha-color-primary-30: "#bf0a00"
  ha-color-primary-40: "#FF0E00"
  ha-color-primary-50: "#ff362a"
  ha-color-primary-60: "#ff5e55"
  ha-color-primary-70: "#ff8680"
  ha-color-primary-80: "#ffafaa"
  ha-color-primary-90: "#ffd7d4"
  ha-color-primary-95: "#ffebea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Ember"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Rose
# Area: Rose  (mdi:flower)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Rose":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/rose.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/rose.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#110a14"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF0033"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF0033"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#200006"
  ha-color-primary-10: "#40000d"
  ha-color-primary-20: "#80001a"
  ha-color-primary-30: "#bf0026"
  ha-color-primary-40: "#FF0033"
  ha-color-primary-50: "#ff2a55"
  ha-color-primary-60: "#ff5577"
  ha-color-primary-70: "#ff8099"
  ha-color-primary-80: "#ffaabb"
  ha-color-primary-90: "#ffd4dd"
  ha-color-primary-95: "#ffeaee"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Rose"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Amber
# Area: Amber  (mdi:weather-sunset)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Amber":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/amber.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/amber.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#100e10"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FF9B00"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FF9B00"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201300"
  ha-color-primary-10: "#402700"
  ha-color-primary-20: "#804e00"
  ha-color-primary-30: "#bf7400"
  ha-color-primary-40: "#FF9B00"
  ha-color-primary-50: "#ffac2a"
  ha-color-primary-60: "#ffbc55"
  ha-color-primary-70: "#ffcd80"
  ha-color-primary-80: "#ffdeaa"
  ha-color-primary-90: "#ffeed4"
  ha-color-primary-95: "#fff7ea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Amber"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Citrine
# Area: Citrine  (mdi:white-balance-sunny)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Citrine":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/citrine.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/citrine.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#100e0b"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FFD400"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFD400"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201a00"
  ha-color-primary-10: "#403500"
  ha-color-primary-20: "#806a00"
  ha-color-primary-30: "#bf9f00"
  ha-color-primary-40: "#FFD400"
  ha-color-primary-50: "#ffdb2a"
  ha-color-primary-60: "#ffe255"
  ha-color-primary-70: "#ffea80"
  ha-color-primary-80: "#fff1aa"
  ha-color-primary-90: "#fff8d4"
  ha-color-primary-95: "#fffbea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Citrine"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Lime
# Area: Lime  (mdi:leaf)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Lime":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/lime.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/lime.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#16150a"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#B8FF03"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B8FF03"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#172000"
  ha-color-primary-10: "#2e4001"
  ha-color-primary-20: "#5c8002"
  ha-color-primary-30: "#8abf02"
  ha-color-primary-40: "#B8FF03"
  ha-color-primary-50: "#c4ff2d"
  ha-color-primary-60: "#d0ff57"
  ha-color-primary-70: "#dcff81"
  ha-color-primary-80: "#e7ffab"
  ha-color-primary-90: "#f3ffd5"
  ha-color-primary-95: "#f9ffea"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Lime"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Verdant
# Area: Verdant  (mdi:sprout)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Verdant":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/verdant.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/verdant.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0b140e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00FF27"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00FF27"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#002005"
  ha-color-primary-10: "#00400a"
  ha-color-primary-20: "#008014"
  ha-color-primary-30: "#00bf1d"
  ha-color-primary-40: "#00FF27"
  ha-color-primary-50: "#2aff4b"
  ha-color-primary-60: "#55ff6f"
  ha-color-primary-70: "#80ff93"
  ha-color-primary-80: "#aaffb7"
  ha-color-primary-90: "#d4ffdb"
  ha-color-primary-95: "#eaffed"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Verdant"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Jade
# Area: Jade  (mdi:diamond-stone)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Jade":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/jade.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/jade.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0d0e10"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#38FFC7"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#38FFC7"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#072019"
  ha-color-primary-10: "#0e4032"
  ha-color-primary-20: "#1c8064"
  ha-color-primary-30: "#2abf95"
  ha-color-primary-40: "#38FFC7"
  ha-color-primary-50: "#59ffd0"
  ha-color-primary-60: "#7affda"
  ha-color-primary-70: "#9cffe3"
  ha-color-primary-80: "#bdffec"
  ha-color-primary-90: "#defff6"
  ha-color-primary-95: "#eefffa"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Jade"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Lagoon
# Area: Lagoon  (mdi:waves)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Lagoon":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/lagoon.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/lagoon.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#060e14"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#25C0FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#25C0FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#051820"
  ha-color-primary-10: "#093040"
  ha-color-primary-20: "#126080"
  ha-color-primary-30: "#1c90bf"
  ha-color-primary-40: "#25C0FF"
  ha-color-primary-50: "#49caff"
  ha-color-primary-60: "#6ed5ff"
  ha-color-primary-70: "#92e0ff"
  ha-color-primary-80: "#b6eaff"
  ha-color-primary-90: "#dbf4ff"
  ha-color-primary-95: "#edfaff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Lagoon"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Azure
# Area: Azure  (mdi:water)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Azure":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/azure.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/azure.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a1116"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#00C2FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#00C2FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001820"
  ha-color-primary-10: "#003040"
  ha-color-primary-20: "#006180"
  ha-color-primary-30: "#0092bf"
  ha-color-primary-40: "#00C2FF"
  ha-color-primary-50: "#2accff"
  ha-color-primary-60: "#55d6ff"
  ha-color-primary-70: "#80e0ff"
  ha-color-primary-80: "#aaebff"
  ha-color-primary-90: "#d4f5ff"
  ha-color-primary-95: "#eafaff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Azure"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Cobalt
# Area: Cobalt  (mdi:hexagon)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Cobalt":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/cobalt.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/cobalt.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a1116"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#007FFF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#007FFF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#001020"
  ha-color-primary-10: "#002040"
  ha-color-primary-20: "#004080"
  ha-color-primary-30: "#005fbf"
  ha-color-primary-40: "#007FFF"
  ha-color-primary-50: "#2a94ff"
  ha-color-primary-60: "#55aaff"
  ha-color-primary-70: "#80bfff"
  ha-color-primary-80: "#aad4ff"
  ha-color-primary-90: "#d4eaff"
  ha-color-primary-95: "#eaf4ff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Cobalt"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Indigo
# Area: Indigo  (mdi:weather-night)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Indigo":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/indigo.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/indigo.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0a0b10"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#5238FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#5238FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#0a0720"
  ha-color-primary-10: "#140e40"
  ha-color-primary-20: "#291c80"
  ha-color-primary-30: "#3e2abf"
  ha-color-primary-40: "#5238FF"
  ha-color-primary-50: "#6f59ff"
  ha-color-primary-60: "#8c7aff"
  ha-color-primary-70: "#a89cff"
  ha-color-primary-80: "#c5bdff"
  ha-color-primary-90: "#e2deff"
  ha-color-primary-95: "#f1eeff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Indigo"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Violet
# Area: Violet  (mdi:shimmer)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Violet":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/violet.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/violet.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#120a10"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#B627FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#B627FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#170520"
  ha-color-primary-10: "#2e0a40"
  ha-color-primary-20: "#5b1480"
  ha-color-primary-30: "#881dbf"
  ha-color-primary-40: "#B627FF"
  ha-color-primary-50: "#c24bff"
  ha-color-primary-60: "#ce6fff"
  ha-color-primary-70: "#da93ff"
  ha-color-primary-80: "#e7b7ff"
  ha-color-primary-90: "#f3dbff"
  ha-color-primary-95: "#f9edff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Violet"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Sand
# Area: Sand  (mdi:beach)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Sand":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/sand.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/sand.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#0f0e0e"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#FFA438"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#FFA438"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#201407"
  ha-color-primary-10: "#40290e"
  ha-color-primary-20: "#80521c"
  ha-color-primary-30: "#bf7b2a"
  ha-color-primary-40: "#FFA438"
  ha-color-primary-50: "#ffb359"
  ha-color-primary-60: "#ffc27a"
  ha-color-primary-70: "#ffd29c"
  ha-color-primary-80: "#ffe1bd"
  ha-color-primary-90: "#fff0de"
  ha-color-primary-95: "#fff7ee"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Sand"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }


#
# Ultimate Art Deco - Graphite
# Area: Graphite  (mdi:circle-slice-8)
# Generated by build/generate_themes.py - do not edit by hand.
#

"Ultimate Art Deco - Graphite":
  modes:
    light:
      primary-background-color: "rgb(24, 27, 38)"
      secondary-background-color: "rgb(24, 27, 38)"
      ha-card-background: "rgba(24, 27, 38, 0.55)"

    dark:
      primary-background-color: "rgb(4, 5, 10)"
      secondary-background-color: "rgb(4, 5, 10)"
      ha-card-background: "rgba(6, 8, 16, 0.52)"


  # =========================================================================
  # BACKGROUND
  # =========================================================================

  ultimate-background: "url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/graphite.webp')"
  background-image: "center / cover no-repeat fixed url('https://cdn.jsdelivr.net/gh/HomeRiz/Home-Assistant-Ultimate-Themes@0.1.2/www/ultimate-theme/backgrounds/art-deco/graphite.webp')"
  lovelace-background: var(--background-image)

  app-theme-color: "#111116"

  # =========================================================================
  # GLASS SYSTEM TOKENS
  # =========================================================================
  ha-card-backdrop-filter: "blur(10px) saturate(1.8) brightness(0.92)"
  ha-card-border-radius: 12px
  ha-card-features-border-radius: var(--ha-card-border-radius)
  ha-card-border-width: 1px
  ha-card-border-color: "rgba(201, 162, 39, 0.55)"
  ha-card-glass-tint: "rgba(6, 8, 16, 0.55)"
  ha-card-glass-sheen: "linear-gradient(160deg, rgba(201,162,39,0.20) 0%, rgba(201,162,39,0.05) 24%, rgba(201,162,39,0) 46%)"
  ha-card-glass-inset-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ha-card-box-shadow: >
    0 0 0 1px color-mix(in srgb, var(--ultimate-glow-color) 30%, transparent), 0 0 18px -6px color-mix(in srgb, var(--ultimate-glow-color) 55%, transparent), 0 0 40px -20px color-mix(in srgb, var(--ultimate-glow-color) 80%, transparent) inset, 0 10px 30px -16px rgba(0,0,0,0.90)
  ultimate-glow-color: "#3838FF"
  ultimate-transition: box-shadow 160ms ease, transform 160ms ease, border-color 160ms ease

  # =========================================================================
  # COLOUR - accent driven by this area
  # =========================================================================
  primary-color: "#3838FF"
  accent-color: var(--primary-color)
  light-primary-color: "#E6F1FF"
  divider-color: "color-mix(in srgb, var(--ultimate-glow-color) 28%, transparent)"
  ha-color-primary-05: "#070720"
  ha-color-primary-10: "#0e0e40"
  ha-color-primary-20: "#1c1c80"
  ha-color-primary-30: "#2a2abf"
  ha-color-primary-40: "#3838FF"
  ha-color-primary-50: "#5959ff"
  ha-color-primary-60: "#7a7aff"
  ha-color-primary-70: "#9c9cff"
  ha-color-primary-80: "#bdbdff"
  ha-color-primary-90: "#dedeff"
  ha-color-primary-95: "#eeeeff"

  # Apple / semantic palette
  red-color: "#FF2D55"
  pink-color: "#FF2D9B"
  purple-color: "#A855F7"
  indigo-color: "#6366F1"
  blue-color: "#00A3FF"
  light-blue-color: "#22D3EE"
  cyan-color: "#00F0FF"
  teal-color: "#14F1C8"
  green-color: "#39FF14"
  yellow-color: "#FFE600"
  orange-color: "#FF8A00"
  brown-color: "#C08552"
  grey-color: "#7A8290"


  # =========================================================================
  # TEXT
  # =========================================================================
  primary-text-color: "rgba(233, 243, 255, 0.97)"
  secondary-text-color: "rgba(160, 176, 200, 0.85)"
  disabled-text-color: "rgba(110, 122, 142, 0.55)"
  text-primary-color: var(--primary-text-color)
  text-dark-color: var(--primary-text-color)
  ha-color-text-secondary: var(--secondary-text-color)
  lumo-body-text-color: var(--primary-text-color)

  # =========================================================================
  # SIDEBAR
  # =========================================================================
  sidebar-background-color: "rgba(4, 5, 10, 0.72)"
  sidebar-icon-color: var(--light-primary-color)
  sidebar-text-color: var(--light-primary-color)
  sidebar-selected-background-color: "color-mix(in srgb, var(--ultimate-glow-color) 18%, transparent)"
  sidebar-selected-icon-color: var(--primary-color)
  sidebar-selected-text-color: var(--sidebar-selected-icon-color)

  # =========================================================================
  # HEADER
  # =========================================================================
  app-header-background-color: "rgba(4, 5, 10, 0.42)"
  app-header-backdrop-filter: var(--ha-card-backdrop-filter)
  app-header-text-color: var(--primary-text-color)
  app-header-edit-background-color: "rgba(0, 0, 0, 0.80)"

  # =========================================================================
  # CARDS / SURFACES
  # =========================================================================
  card-background-color: "rgba(10, 12, 22, 0.94)"
  paper-card-background-color: var(--ha-card-background)
  paper-listbox-background-color: "rgba(10, 12, 22, 0.94)"
  rgb-card-background-color: "rgb(6, 8, 16)"
  clear-background-color: var(--ha-card-background)
  mdc-theme-surface: "rgba(10, 12, 22, 0.94)"
  material-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-background-color: "rgba(10, 12, 22, 0.94)"
  table-row-alternative-background-color: "rgba(18, 21, 34, 0.94)"
  md-list-container-color: none

  # =========================================================================
  # DIALOGS
  # =========================================================================
  ha-dialog-surface-background: "rgba(8, 10, 20, 0.86)"
  
  ha-dialog-surface-backdrop-filter: none
  ha-dialog-scrim-backdrop-filter: blur(2px)
  dialog-box-shadow: var(--ha-card-box-shadow)
  paper-dialog-background-color: "rgba(8, 10, 20, 0.86)"
  more-info-header-background: "rgba(4, 5, 10, 0.42)"
  mdc-dialog-scrim-color: "rgba(0, 0, 0, 0.78)"

  # =========================================================================
  # STATES, SLIDERS, SWITCHES
  # =========================================================================
  state-icon-color: var(--primary-text-color)
  state-icon-active-color: var(--primary-color)
  state-icon-unavailable-color: var(--disabled-text-color)
  state-inactive-color: var(--disabled-text-color)
  paper-item-icon-color: var(--primary-text-color)
  paper-item-icon-active-color: var(--state-icon-active-color)
  paper-slider-knob-color: "#FFFFFF"
  paper-slider-knob-start-color: var(--paper-slider-knob-color)
  paper-slider-pin-color: var(--paper-slider-knob-color)
  paper-slider-active-color: var(--primary-color)
  paper-slider-secondary-color: var(--paper-slider-knob-color)
  paper-slider-container-color: "rgba(160, 176, 200, 0.20)"
  ha-slider-background: none !important
  switch-checked-track-color: var(--primary-color)
  switch-checked-button-color: "#FFFFFF"
  switch-unchecked-track-color: var(--disabled-text-color)
  switch-unchecked-button-color: "#FFFFFF"
  paper-toggle-button-checked-bar-color: var(--primary-color)
  paper-toggle-button-checked-button-color: "#FFFFFF"
  paper-toggle-button-unchecked-bar-color: var(--disabled-text-color)
  paper-toggle-button-unchecked-button-color: "#FFFFFF"

  # =========================================================================
  # BADGES / LABELS
  # =========================================================================
  ha-badge-border-radius: 8px
  label-badge-background-color: "rgba(10, 12, 22, 0.94)"
  label-badge-text-color: var(--primary-text-color)
  label-badge-red: var(--red-color)

  # =========================================================================
  # INPUTS / FORMS
  # =========================================================================
  input-ink-color: var(--primary-text-color)
  input-fill-color: transparent
  input-disabled-fill-color: transparent
  input-label-ink-color: var(--primary-text-color)
  input-disabled-ink-color: var(--disabled-text-color)
  input-dropdown-icon-color: var(--primary-text-color)
  input-idle-line-color: var(--secondary-text-color)
  input-hover-line-color: var(--primary-color)
  ha-color-form-background: var(--ha-card-background)
  ha-color-form-background-hover: var(--ha-card-background)
  ha-color-form-background-disabled: var(--ha-card-background)
  mdc-checkbox-unchecked-color: var(--secondary-text-color)
  mdc-radio-unchecked-color: var(--secondary-text-color)
  codemirror-property: var(--accent-color)
  markdown-code-background-color: "rgba(0, 0, 0, 0.70)"
  code-editor-background-color: "rgba(0, 0, 0, 0.70)"

  # =========================================================================
  # DROPDOWNS (HA 2025.x web-awesome)
  # =========================================================================
  wa-shadow-m: var(--ha-card-box-shadow)
  wa-border-radius-s: var(--ha-card-border-radius)
  wa-border-radius-m: 12px
  wa-border-width-s: 0px
  wa-color-neutral-fill-normal: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-on-neutral-normal: var(--primary-text-color)
  ha-color-fill-primary-quiet-resting: "color-mix(in srgb, var(--ultimate-glow-color) 14%, transparent)"
  ha-color-fill-primary-quiet-hover: "color-mix(in srgb, var(--ultimate-glow-color) 26%, transparent)"

  # =========================================================================
  # MUSHROOM geometry tokens
  # =========================================================================
  mush-spacing: 8px
  mush-icon-border-radius: 8px
  mush-control-border-radius: 8px
  mush-chip-border-radius: 8px
  mush-chip-background: var(--ha-card-background)
  mush-rgb-white: 255, 255, 255
  mush-title-font-weight: 600
  mush-title-letter-spacing: 0.02em
  ha-tile-icon-border-radius: 8px

  # =========================================================================
  # MINI MEDIA PLAYER / MISC CUSTOM CARDS
  # =========================================================================
  mcg-title-letter-spacing: 0.15em
  mini-media-player-base-color: var(--primary-text-color)
  mini-media-player-icon-color: var(--primary-text-color)

  # =========================================================================
  # CARD-MOD
  # =========================================================================
  card-mod-theme: "Ultimate Art Deco - Graphite"

  card-mod-root: |
    /* ---- iOS-safe fixed background ------------------------------------
       background-attachment: fixed is janky/ignored on iOS Safari and the HA
       companion app. Painting the backdrop onto a fixed pseudo-element gives
       the same parallax-free result everywhere. */
    :host::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
    }
    :host::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%);
      z-index: -1;
      pointer-events: none;
    }

    /* ---- HA colour tokens ---------------------------------------------- */
    :host {
      --token-rgb-primary: 0, 163, 255;
      --token-rgb-black: 0, 0, 0;
      --token-rgb-white: 255, 255, 255;
      --token-rgb-purple: 168, 85, 247;
      --token-rgb-pink: 255, 45, 155;
      --token-rgb-red: 255, 45, 85;
      --token-rgb-deep-purple: 168, 85, 247;
      --token-rgb-indigo: 99, 102, 241;
      --token-rgb-blue: 0, 163, 255;
      --token-rgb-light-blue: 34, 211, 238;
      --token-rgb-cyan: 0, 240, 255;
      --token-rgb-teal: 20, 241, 200;
      --token-rgb-green: 57, 255, 20;
      --token-rgb-light-green: 57, 255, 20;
      --token-rgb-lime: 57, 255, 20;
      --token-rgb-yellow: 255, 230, 0;
      --token-rgb-amber: 255, 230, 0;
      --token-rgb-orange: 255, 138, 0;
      --token-rgb-deep-orange: 255, 138, 0;
      --token-rgb-brown: 192, 133, 82;
      --token-rgb-grey: 142, 142, 147;
      --token-rgb-blue-grey: 120, 130, 145;
      --token-rgb-state-icon: 255, 255, 255;
      --token-size-radius-card: var(--ha-card-border-radius);
    }

    /* ---- scrollbars ---------------------------------------------------- */
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb {
      background: color-mix(in srgb, var(--ultimate-glow-color) 40%, transparent);
      border-radius: 8px;
    }
    ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

  card-mod-card: |
    /* ================= BASE RESET ================= */
    ha-card {
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      border: 1px solid var(--ha-card-border-color);
      transition: var(--ultimate-transition);
    }

    /* ================= GLASS LAYER =================
       Sits behind the card content so blur never touches text. */
    ha-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--ha-card-glass-tint);
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      z-index: -1;
      pointer-events: none;
      border-radius: inherit;
      box-shadow: var(--ha-card-glass-inset-shadow);
    }

    /* ================= SPECULAR SHEEN =================
       Real glass has a bright top rim. This is the single biggest visual
       upgrade over the source themes, which only blur. */
    ha-card::after {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
      background: var(--ha-card-glass-sheen);
      z-index: 0;
      mix-blend-mode: screen;
    }
    ha-card > * { position: relative; z-index: 1; }

    /* ================= HOVER LIFT ================= */
    @media (hover: hover) and (prefers-reduced-motion: no-preference) {
      ha-card:hover {
        transform: translateY(-1px);
      }
      ha-card:hover::before {
        box-shadow: var(--ha-card-glass-inset-shadow),
                    0 10px 28px -14px rgba(0, 0, 0, 0.75);
      }
    }

    /* ================= NO-BACKDROP-FILTER FALLBACK =================
       Old WebViews (some Android companion builds) do not support
       backdrop-filter. Without this the cards render fully transparent
       and become unreadable over the background image. */
    @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      ha-card::before {
        background: rgba(10, 12, 22, 0.94);
      }
    }

    /* ================= EXCLUSIONS =================
       Cards that must stay flat, or that draw their own surface. */
    :host(hui-heading-card) ha-card,
    :host(hui-glance-card) ha-card,
    :host(mushroom-title-card) ha-card,
    :host(mushroom-chips-card) ha-card,
    :host(.type-custom-bubble-card) ha-card,
    ha-card.text-only {
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      box-shadow: none !important;
      border: none !important;
    }
    :host(hui-heading-card) ha-card::before,
    :host(hui-heading-card) ha-card::after,
    :host(hui-glance-card) ha-card::before,
    :host(hui-glance-card) ha-card::after,
    :host(mushroom-title-card) ha-card::before,
    :host(mushroom-title-card) ha-card::after,
    :host(mushroom-chips-card) ha-card::before,
    :host(mushroom-chips-card) ha-card::after,
    :host(.type-custom-bubble-card) ha-card::before,
    :host(.type-custom-bubble-card) ha-card::after,
    ha-card.text-only::before,
    ha-card.text-only::after {
      content: none !important;
      background: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
    }
    :host(.type-custom-bubble-card) ha-card {
      border-radius: 0px !important;
    }

    /* ================= DATA TABLES ================= */
    .mdc-data-table,
    .mdc-data-table__row,
    ha-sankey-chart {
      background: none !important;
    }
    .mdc-data-table__header-cell {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter) !important;
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter) !important;
    }

  card-mod-sidebar: |
    /* The blur goes on the sidebar's inner elements, never on `:host`.

       `backdrop-filter` on the `ha-sidebar` host corrupts the layout of an
       iframe panel rendered next to it. Chrome composites the blurred host onto
       its own layer and the iframe's document starts resolving positions
       against a stale coordinate space, so elements get painted at an origin
       their own parent does not agree with. In HACS that means a dialog whose
       contents spill outside the box with the version dropdown floating loose.

       Measured on the HACS download dialog - dialog surface always [525,197],
       the numbers are where its children land:

         blur on :host                        [136,33]   broken, 3/3
         + will-change: transform on iframe   [136,33]   broken
         + translateZ(0) on ha-panel-custom   [136,33]   broken
         + translateZ(0) on the iframe        flaky - passed once, failed once
         + contain: paint on the sidebar      [136,33]   broken, 2/2
         blur on .menu / .panels-list         [525,250]  correct, 4/4

       Promoting the iframe to its own layer looked like it worked and did not
       survive retesting; only moving the filter off the host does. The visual
       result is identical - those two elements cover the whole sidebar.

       This only became visible once `extra_module_url` was added, because that
       is what makes card-mod run outside Lovelace: before it, the sidebar was
       never blurred on a panel like HACS in the first place. */
    :host {
      background: none !important;
    }
    .menu,
    .panels-list {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }
    ha-list-item-button,
    ha-list-item-button.selected::before,
    ha-md-list-item,
    ha-md-list-item.selected::before,
    paper-icon-item {
      border-radius: var(--ha-card-border-radius) !important;
      --ha-list-item-focus-radius: var(--ha-card-border-radius) !important;
    }
    ha-md-list-item.selected,
    paper-icon-item.iron-selected {
      background: var(--ha-color-fill-primary-quiet-resting) !important;
    }

  

  card-mod-top-app-bar-fixed: |
    /* The header sits above every panel, not just dashboards. */
    :host {
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
    }

  card-mod-dialog: |
    /* more-info, settings dialogs, pickers.

       No `backdrop-filter` here either, for the same reason as
       `ha-dialog-surface-backdrop-filter` above: it would make the dialog a
       containing block and throw every dropdown opened inside it out of place.
       Dialogs get their depth from `ha-dialog-surface-background` and from the
       blurred scrim behind them instead. */
    :host {
      --dialog-backdrop-filter: none;
    }

  card-mod-config: |
    /* Settings, Developer Tools, History - every built-in non-Lovelace panel.

       These must paint the backdrop themselves. `card-mod-root` is injected
       into `hui-root`, which is the *Lovelace* root - it does not exist here,
       so its fixed pseudo-element can never reach this panel.

       Three things this block gets deliberately right, each of which was wrong
       in 0.0.1 and cost an evening to find:

       1. Not `:host`. `ha-panel-config` has no shadow root of its own, so
          card-mod falls back to the shadow root of `home-assistant-main`.
          `:host` there resolves to `home-assistant-main`, whose background is
          covered by the opaque `ha-drawer` on top of it. Target elements in
          that tree by name instead.

       2. Not a negative-z pseudo-element. There is no stacking context here to
          sit behind, so `z-index: -2` puts the layer behind the document
          background and it never renders.

       3. Paint on `ha-drawer`, not on the panel. The drawer spans the whole
          viewport - sidebar included - so the sidebar's own backdrop-filter
          finally has an image to blur. Painting only `ha-panel-config` gives
          you a backdrop in the content area and a flat grey sidebar beside it,
          which is exactly the seam this fixes. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-config,
    partial-panel-resolver {
      background: transparent !important;
    }
    ha-card {
      background: var(--ha-card-glass-tint) !important;
      backdrop-filter: var(--ha-card-backdrop-filter);
      -webkit-backdrop-filter: var(--ha-card-backdrop-filter);
      border-radius: var(--ha-card-border-radius) !important;
      box-shadow: var(--ha-card-glass-inset-shadow) !important;
    }

  card-mod-panel-custom: |
    /* Custom panels. They render their own frontend inside an iframe or their
       own shadow tree, so only the outer surface is reachable - but that is
       enough to carry the backdrop. Same reasoning as `card-mod-config` above:
       paint the drawer, not the panel.

       The sidebar keeps its blur here. `backdrop-filter` on the `ha-sidebar`
       host used to corrupt the layout of the iframe rendered beside it; that is
       fixed in `card-mod-sidebar` above, by putting the filter on the sidebar's
       inner elements instead of its host. Promoting the iframe to its own layer
       was tried first and did not hold up - see the measurements there. */
    ha-drawer {
      background-image: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.55) 70%, rgba(0,0,0,0.78) 100%), var(--ultimate-background);
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }
    ha-panel-custom,
    partial-panel-resolver {
      background: transparent !important;
    }

  card-mod-view: |
    /* Per-view background override hook.
       Set --ultimate-view-background in a view's card_mod to give that single
       tab its own backdrop - see docs/PER-VIEW-BACKGROUNDS.md

       This block is also what makes a per-view `theme:` change the backdrop.
       Home Assistant applies a view's theme to `hui-view-container`, which is
       *below* `hui-root` - so the backdrop `card-mod-root` paints on the root
       always resolves to the profile theme, no matter what the tab is set to.
       Colours changed per tab, the image never did. Painting here instead puts
       the backdrop inside the scope the view theme actually reaches.

       Not `:host`: `hui-view` has no shadow root, so card-mod injects this into
       `hui-root`'s shadow root, where `:host` is `hui-root` - the wrong element,
       with the wrong theme in scope. Target `hui-view` by name.

       The fallback chain means this is a no-op where nothing is overridden: an
       explicit --ultimate-view-background wins, otherwise the theme in scope at
       view level, which on an unthemed tab is the same image already painted
       underneath. */
    hui-view::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: var(--ultimate-view-background, var(--ultimate-background));
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      z-index: -2;
      pointer-events: none;
      opacity: var(--ultimate-view-background-opacity, 1);
    }
